# Fase 3.12 — Plan atómico Deals module

> Ejecutado en sesión 2026-04-23 (post-aprobación de plan mode).
> 4 olas consolidadas en un único pase (ver `3-12-SUMMARY.md`).

<phase id="3.12" name="Deals module">

<task type="auto" wave="1">
  <name>Tipos + data layer + seed config</name>
  <files>
    src/lib/config.ts,
    src/lib/deals.ts,
    clients/default/config.json,
    clients/_template/config.json,
    clients/demo-cliente-a/config.json
  </files>
  <action>
    Añadir `Deal` + `HomeDealsModule` a `HomeModuleVariant`.
    Crear `src/lib/deals.ts` con pipeline filter/sort/search + `filterActiveDeals` +
    `DEAL_SORT_OPTIONS` + `formatDealExpiry` + `todayISO`.
    Poblar 20 deals seed en `clients/default/config.json` con covers Unsplash y
    featureCatalog de 8 categorías. Mirror de 15 textos `deals_*` en
    `_template` y `demo-cliente-a` (este con traducciones ES).
  </action>
  <verify>
    pnpm check limpio.
    `node -e "JSON.parse(fs.readFileSync(...))"` pasa en los 3 config.json.
  </verify>
  <done>
    Tipos expuestos sin errores de TS. Config con 20 deals parseables.
    Textos accesibles vía `getConfig().textos.deals_*`.
  </done>
</task>

<task type="auto" wave="2">
  <name>Listing + toolbar + filter + sort + search + ruta</name>
  <files>
    src/components/deals/deals-module.tsx,
    src/components/deals/deals-grid.tsx,
    src/components/deals/deal-card.tsx,
    src/components/deals/deals-filter-overlay.tsx,
    src/app/(kiosk)/home/[module]/page.tsx,
    src/app/(kiosk)/home/[module]/[slug]/page.tsx
  </files>
  <action>
    Crear módulo con hero + `ListingsToolbar` reusado + grid 3-col +
    `DealsFilterOverlay` (1 sección features) + `SortOverlay` reusado con
    `DEAL_SORT_OPTIONS` + `SearchOverlay` reusado con adapter que intercepta
    el Link para disparar CustomEvent en vez de navegar. `DealCard` dispara
    `CustomEvent('kiosk:deal-redeem-open', { dealSlug })`.
    Añadir rama `case 'deals'` en `[module]/page.tsx` con `<AdsSlot>`.
    Añadir `if (kind === 'deals') notFound()` en `[slug]/page.tsx`.
  </action>
  <verify>
    pnpm check limpio. Playwright MCP `/home/deals` muestra grid con 20
    cards sorted ascending por expiresAt. Filter overlay abre. Sort overlay
    abre. Search overlay abre.
  </verify>
  <done>
    Render pixel-perfect vs `Deals.png` y `Deals-Filter.png` (±3px).
  </done>
</task>

<task type="auto" wave="3">
  <name>Modal redeem + host + send flows</name>
  <files>
    src/components/deals/deal-redeem-modal.tsx,
    src/components/deals/deal-redeem-host.tsx,
    src/components/deals/deals-module.tsx
  </files>
  <action>
    `DealRedeemModal` verbatim SVG: cover con title+expiry overlay, headline,
    subtitle, longDescription, promoCode pill opcional, QR 240×240 con logo
    opcional (`resolveAssetPath` normaliza paths relativos), 2 botones SEND
    side-by-side, CANCEL link.
    `DealRedeemHost` con máquina de estados `closed | redeem | send-phone |
    send-email | sent`. Escucha `CustomEvent('kiosk:deal-redeem-open')`.
    Delega a `SendToPhoneModal` / `SendToEmailModal` / `SendConfirmationPopup`
    de listings.
    Montar host en `DealsModule`.
  </action>
  <verify>
    pnpm check limpio. Playwright MCP: tap card → modal con data correcta.
    SEND TO PHONE → NumericKeypad. SEND TO EMAIL → QWERTY. SendConfirmationPopup
    tras submit válido.
  </verify>
  <done>
    Match visual vs `Deals–Details_Send.png` (±3px). Phone y Email flows
    completos end-to-end.
  </done>
</task>

<task type="auto" wave="4">
  <name>QA cierre + docs</name>
  <files>
    .planning/3-12-SUMMARY.md,
    .planning/3-12-COVERAGE.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    docs/superpowers/specs/2026-04-23-deals-module-design.md
  </files>
  <action>
    Escribir spec formal, SUMMARY, COVERAGE.
    Ejecutar auditor-white-label sobre `src/components/deals/`.
    Actualizar STATE y ROADMAP con Fase 3.12 cerrada.
  </action>
  <verify>
    Auditor sin strings nuevos hardcoded (fallbacks `??` toleradas).
    STATE y ROADMAP incluyen Fase 3.12 cerrada.
  </verify>
  <done>
    Commit único con los 4 tasks. Listing, filter, modal redeem, send flow
    todos verificados visualmente. Fase cerrada.
  </done>
</task>

</phase>
