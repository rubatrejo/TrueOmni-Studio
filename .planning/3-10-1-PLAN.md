# Plan 3-10-1 — Módulo Passes (listado + detail + share con QR + sent confirmation)

**Fase:** 3.10 · **Orden:** 1 · **Depende de:** fases 3.2-3.9 commiteadas.
**Cubre requerimientos:** R1 (pantallas), R2 (white-label), R7 (UX home), R8 (modales).
**Skills necesarios al ejecutar:** Tier 1 obligatorio (`frontend-design`, `ui-ux-pro-max`, `theme-factory`). Tier 2 (`web-design-guidelines`) al cerrar.
**Spec de referencia:** `docs/superpowers/specs/2026-04-22-passes-module-design.md` (commit `9bed5ae`).

**Resumen:** Se implementa el módulo Passes como 6º discriminador `kind: 'passes'` con ruta listado `/home/passes` + ruta detail `/home/passes/{slug}` + modal Share con QR escaneable + pantalla Sent Confirmation. Reuso máximo del ecosistema `listings/` (SendModalChrome / TermsCheckbox / NumericKeypad / CancelSendButtons / useEscapeToClose / FloatingHomeButton / BackButton) + SearchOverlay del home. Nueva dep `qrcode.react` (~3 KB gzip) para el QR. Cero hardcoded (todos los strings en `config.textos.passes_*`, todo el data en `config.features.home.modules.passes`). 16 tasks en 4 olas con 4 commits finales.

**Orden de ejecución:**

- Ola 1 (Foundations): T1-T4. Tipos + seed + lib + dep + commit.
- Ola 2 (Listado): T5-T9. Card + grid + toolbar + module + ruta + commit.
- Ola 3 (Detail + Activities): T10-T12. Activity row + detail + ruta slug + commit.
- Ola 4 (Share + Sent + QA): T13-T16. Share modal + sent confirmation + verificación + commit.

---

<task type="auto">
  <name>Añadir tipos PassActivity, PassItem, HomePassesModule a src/lib/config.ts</name>
  <files>src/lib/config.ts</files>
  <action>
    Abrir `src/lib/config.ts`. Localizar el bloque donde están definidos `HomeMapModule` / `HomeDigitalBrochureModule` / `HomeEventsModule` (interfaces de los kinds existentes). Añadir ANTES de la union `HomeModuleVariant`:

    ```ts
    /** Actividad incluida en un pass — display-only (title + image + desc + website). */
    export interface PassActivity {
      slug: string;
      title: string;
      /** Path relativo o URL absoluta del thumbnail. */
      image: string;
      description: string;
      /** Link externo que se abre al tap "View Website". */
      website: string;
    }

    /** Pass individual — paquete de actividades regalable/comprable. */
    export interface PassItem {
      slug: string;
      title: string;
      /** Cover del pass — path relativo a `clients/{slug}/assets/` o URL absoluta. */
      cover: string;
      /** URL al pass en Bandwango. Codificado en el QR del share. */
      bandwangoUrl: string;
      /** Tagline corto opcional para la card y el hero del detail. */
      tagline?: string;
      activities: PassActivity[];
    }

    /** Módulo Passes — kind discriminator 'passes'. */
    export interface HomePassesModule {
      kind: 'passes';
      label: string;
      heroImage: string;
      passes: PassItem[];
    }
    ```

    Luego localizar `export type HomeModuleVariant =` y añadir `| HomePassesModule` como última alternativa. El tipo final queda:

    ```ts
    export type HomeModuleVariant =
      | HomeModule
      | HomeEventsModule
      | HomeSocialWallModule
      | HomeDigitalBrochureModule
      | HomeMapModule
      | HomePassesModule;
    ```

    **No tocar** nada más en el archivo.

  </action>
  <verify>
    - `pnpm typecheck` pasa limpio.
    - `grep -n "HomePassesModule\|PassItem\|PassActivity" src/lib/config.ts` muestra los 3 exports.
    - `grep -n "HomePassesModule" src/lib/config.ts` muestra 2 líneas (interface + union).
  </verify>
  <done>
    Tres tipos exportados. Union actualizada. Typecheck limpio.
  </done>
</task>

<task type="auto">
  <name>Seed del módulo passes + strings passes_* en clients/default/config.json</name>
  <files>clients/default/config.json</files>
  <action>
    Abrir `clients/default/config.json`. Localizar `features.home.modules` (ya contiene `restaurants`, `things-to-do`, `events`, etc.). Añadir al final del objeto `modules`, antes del cierre `}`, el bloque `passes`:

    ```jsonc
    "passes": {
      "kind": "passes",
      "label": "Passes",
      "heroImage": "https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?w=1920&q=80",
      "passes": [
        {
          "slug": "museum-pass",
          "title": "Museum Pass",
          "cover": "https://images.unsplash.com/photo-1565060169861-2d4b4e54c8c7?w=1200&q=80",
          "bandwangoUrl": "https://app.bandwango.com/p/museum-pass",
          "tagline": "Skip the line at 8 iconic museums.",
          "activities": [
            {
              "slug": "cheyenne-depot-museum",
              "title": "Cheyenne Depot Museum",
              "image": "https://images.unsplash.com/photo-1565060169861-2d4b4e54c8c7?w=600&q=80",
              "description": "Designated a National Historic Landmark, this former Union Pacific Depot has been restored to its original glory. Home to a visitor center and restaurant, the Depot also features the Cheyenne Depot Museum, rich with railroad history, exhibits and interactive displays, including an intricate working model.",
              "website": "https://example.com/cheyenne-depot"
            },
            {
              "slug": "heard-museum",
              "title": "Heard Museum",
              "image": "https://images.unsplash.com/photo-1566054757965-8c4085344c96?w=600&q=80",
              "description": "The Heard Museum advances American Indian art by presenting the stories of American Indian people from a first-person perspective. Founded in 1929, the Heard is internationally recognized for the quality of its collections and educational programming.",
              "website": "https://example.com/heard"
            },
            {
              "slug": "phoenix-art-museum",
              "title": "Phoenix Art Museum",
              "image": "https://images.unsplash.com/photo-1580136579312-94651dfd596d?w=600&q=80",
              "description": "The largest art museum in the Southwestern United States, Phoenix Art Museum features a collection of more than 20,000 works of American, Asian, European, Latin American, Western American, modern and contemporary art.",
              "website": "https://example.com/phoenix-art"
            },
            {
              "slug": "musical-instrument-museum",
              "title": "Musical Instrument Museum",
              "image": "https://images.unsplash.com/photo-1466428996289-fb355538da1b?w=600&q=80",
              "description": "MIM is the world's only global musical instrument museum, offering guests a rich musical journey with nearly 7,000 instruments on display from around 200 countries and territories across the globe.",
              "website": "https://example.com/mim"
            }
          ]
        },
        {
          "slug": "city-tour-pass",
          "title": "City Tour Pass",
          "cover": "https://images.unsplash.com/photo-1520583457224-aee11bad5112?w=1200&q=80",
          "bandwangoUrl": "https://app.bandwango.com/p/city-tour-pass",
          "tagline": "5 unforgettable tours in one pass.",
          "activities": [
            {
              "slug": "red-bus-tour",
              "title": "Red Bus City Tour",
              "image": "https://images.unsplash.com/photo-1544620347-c4fd4a3d5957?w=600&q=80",
              "description": "Hop-on hop-off double decker bus tour with 12 stops at the city's top landmarks. Audio guide available in 8 languages. Tickets valid 24 hours from first boarding.",
              "website": "https://example.com/red-bus"
            },
            {
              "slug": "walking-food-tour",
              "title": "Historic Walking Tour",
              "image": "https://images.unsplash.com/photo-1519501025264-65ba15a82390?w=600&q=80",
              "description": "Two-hour guided walking tour through the historic downtown district. Learn about the architecture, culture and hidden gems from our expert local guides. Small groups, max 12 people.",
              "website": "https://example.com/walking"
            },
            {
              "slug": "river-cruise",
              "title": "Sunset River Cruise",
              "image": "https://images.unsplash.com/photo-1507525428034-b723cf961d3e?w=600&q=80",
              "description": "90-minute narrated cruise along the river at sunset. Complimentary drink included. Live music on Friday and Saturday evenings. Reservation recommended.",
              "website": "https://example.com/cruise"
            },
            {
              "slug": "bike-rental",
              "title": "All-day Bike Rental",
              "image": "https://images.unsplash.com/photo-1485965120184-e220f721d03e?w=600&q=80",
              "description": "Full-day cruiser bike rental from 8am to 8pm. Helmet, lock and city map included. Perfect for exploring the riverfront trail and downtown.",
              "website": "https://example.com/bikes"
            }
          ]
        },
        {
          "slug": "food-drink-pass",
          "title": "Food & Drink Pass",
          "cover": "https://images.unsplash.com/photo-1414235077428-338989a2e8c0?w=1200&q=80",
          "bandwangoUrl": "https://app.bandwango.com/p/food-drink-pass",
          "tagline": "Taste the best of the city — 10 stops.",
          "activities": [
            {
              "slug": "daddy-of-the-malt",
              "title": "Daddy of the Malt",
              "image": "https://images.unsplash.com/photo-1535958636474-b021ee887b13?w=600&q=80",
              "description": "Craft brewery and tasting room with rotating selection of 16 house beers. Your pass includes a flight of 4 beers of your choice. Kid-friendly patio with lawn games.",
              "website": "https://example.com/malt"
            },
            {
              "slug": "farm-to-table",
              "title": "Farm to Table Bistro",
              "image": "https://images.unsplash.com/photo-1414235077428-338989a2e8c0?w=600&q=80",
              "description": "Seasonal menu sourced from local farms. Your pass gets you a complimentary appetizer with any entrée. Reservations recommended for dinner.",
              "website": "https://example.com/farm"
            },
            {
              "slug": "coffee-roasters",
              "title": "Central Coffee Roasters",
              "image": "https://images.unsplash.com/photo-1495474472287-4d71bcdd2085?w=600&q=80",
              "description": "Award-winning coffee roaster and café. Pass includes a 12oz specialty drink of your choice. Bean tours available on weekends.",
              "website": "https://example.com/coffee"
            },
            {
              "slug": "gelato-stand",
              "title": "Nonna's Gelato",
              "image": "https://images.unsplash.com/photo-1551024601-bec78aea704b?w=600&q=80",
              "description": "Authentic Italian gelato made fresh daily from a family recipe. Your pass includes one regular-size cup or cone with two flavors.",
              "website": "https://example.com/gelato"
            }
          ]
        }
      ]
    }
    ```

    Luego en el bloque `textos` (top-level del JSON), añadir al final (antes del cierre) los strings `passes_*`:

    ```jsonc
    "passes_label": "Passes",
    "passes_get_yours": "GET YOURS",
    "passes_view_website": "View Website",
    "passes_share_instruction": "SCAN THIS QR CODE TO HAVE YOUR PASS",
    "passes_share_phone_label": "Enter your phone number",
    "passes_share_country": "USA (+1)",
    "passes_share_phone_placeholder": "000-555-0115",
    "passes_share_terms": "I accept details",
    "passes_share_send": "SEND",
    "passes_share_cancel": "CANCEL",
    "passes_sent_title": "You are all set!",
    "passes_sent_message": "The email was successfully sent",
    "passes_sent_redirect_label": "Returning in {seconds}s…"
    ```

    Verificar después que el JSON sigue siendo válido (comas, brackets, comillas).

  </action>
  <verify>
    - `node -e "JSON.parse(require('fs').readFileSync('clients/default/config.json'))"` no lanza error.
    - `grep -n '"passes":' clients/default/config.json` muestra el bloque dentro de `modules`.
    - `grep -c '"passes_' clients/default/config.json` devuelve ≥13 (los 13 textos `passes_*`).
    - `pnpm typecheck` pasa limpio (los tipos añadidos en task 1 validan el seed).
  </verify>
  <done>
    JSON válido.
    `features.home.modules.passes.kind === 'passes'` y tiene 3 passes con 4 activities cada uno.
    13 strings `passes_*` en `textos`.
    Typecheck limpio.
  </done>
</task>

<task type="auto">
  <name>Instalar qrcode.react + crear src/lib/passes.ts con helpers de validación y dispatch</name>
  <files>package.json, pnpm-lock.yaml, src/lib/passes.ts</files>
  <action>
    **Paso 1:** Instalar dep:

    ```bash
    pnpm add qrcode.react
    ```

    Versión esperada: `^3.1.0` o superior. La lib expone `QRCodeSVG` y `QRCodeCanvas` como named exports.

    **Paso 2:** Crear `src/lib/passes.ts`:

    ```ts
    /** Resultado del share de un pass, serializable, listo para dispatch. */
    export interface PassShareResult {
      /** ISO 8601 UTC timestamp. */
      timestamp: string;
      /** Slug del KIOSK_CLIENT activo. */
      client: string;
      passSlug: string;
      passTitle: string;
      /** Teléfono formateado "+1-XXX-XXX-XXXX". */
      phone: string;
      /** URL del pass en Bandwango (viene del config). */
      bandwangoUrl: string;
    }

    /** Valida phone: sólo dígitos, min 10. Patrón del send-to-phone-modal. */
    export function isValidPhone(raw: string): boolean {
      const digits = raw.replace(/\D/g, '');
      return digits.length >= 10;
    }

    /** Formatea phone de 10 dígitos a "+1-XXX-XXX-XXXX". */
    export function formatPhone(raw: string, countryCode = '+1'): string {
      const digits = raw.replace(/\D/g, '').slice(-10);
      if (digits.length !== 10) return `${countryCode}-${digits}`;
      return `${countryCode}-${digits.slice(0, 3)}-${digits.slice(3, 6)}-${digits.slice(6)}`;
    }

    /** Construye el resultado final del share. */
    export function buildShareResult(params: {
      client: string;
      passSlug: string;
      passTitle: string;
      phone: string;
      bandwangoUrl: string;
    }): PassShareResult {
      return {
        timestamp: new Date().toISOString(),
        client: params.client,
        passSlug: params.passSlug,
        passTitle: params.passTitle,
        phone: formatPhone(params.phone),
        bandwangoUrl: params.bandwangoUrl,
      };
    }

    /** Dispatch v1: console + CustomEvent. Sin persistencia ni HTTP. */
    export function dispatchShareResult(result: PassShareResult): void {
      // eslint-disable-next-line no-console
      console.log('[kiosk:pass-share]', result);
      if (typeof window !== 'undefined') {
        window.dispatchEvent(new CustomEvent('kiosk:pass-shared', { detail: result }));
      }
    }
    ```

    **No importar `server-only`** — se usa en client components.

  </action>
  <verify>
    - `cat package.json | grep qrcode.react` muestra la dep instalada.
    - `pnpm typecheck` pasa limpio.
    - `pnpm lint` sin errores en `src/lib/passes.ts`.
    - `grep -n "export" src/lib/passes.ts` muestra: `PassShareResult`, `isValidPhone`, `formatPhone`, `buildShareResult`, `dispatchShareResult`.
  </verify>
  <done>
    Dep `qrcode.react` instalada.
    `src/lib/passes.ts` con 5 exports, typecheck/lint limpios.
  </done>
</task>

<task type="auto">
  <name>Commit ola 1 (foundations)</name>
  <files>src/lib/config.ts, clients/default/config.json, src/lib/passes.ts, package.json, pnpm-lock.yaml</files>
  <action>
    ```bash
    git add src/lib/config.ts clients/default/config.json src/lib/passes.ts package.json pnpm-lock.yaml
    git commit -m "feat(passes): tipos + seed default + lib/passes + qrcode.react

Fase 3.10 ola 1 (foundations). Añade discriminador 'passes' al
HomeModuleVariant, seed de 3 passes (Museum / City Tour / Food &
Drink) con 4 activities cada uno, 13 strings passes\_\* en textos,
helpers de validación + dispatch v1 (console + CustomEvent). Nueva
dep qrcode.react para el QR del share modal."

````</action>
  <verify>
    -`git log -1 --oneline`muestra el commit.
    -`git status --short` limpio de los 5 archivos.
</verify>
<done>Commit creado. Tree limpio de los 5 archivos.</done>
</task>

<task type="auto">
  <name>Crear pass-card: imagen cover + overlay azul top + título centrado</name>
  <files>src/components/passes/pass-card.tsx</files>
  <action>
    Crear el archivo. Card = `Link` a `/home/passes/{slug}`, 898×400, cover con overlay azul en la banda superior (donde va el título) + título bold uppercase blanco centrado. Fallback si la imagen falla.

    ```tsx
    'use client';

    import Link from 'next/link';
    import { useState } from 'react';

    import type { PassItem } from '@/lib/config';

    interface Props {
      pass: PassItem;
    }

    /**
     * Card del pass en el listado. 898×400 full-width dentro del canvas.
     * Cover fill + overlay azul rounded-top + título bold 56px uppercase blanco
     * sobre el overlay. Tap → detail del pass.
     */
    export function PassCard({ pass }: Props) {
      const [imgError, setImgError] = useState(false);
      return (
        <Link
          href={`/home/passes/${pass.slug}`}
          className="relative block overflow-hidden focus:outline-none focus-visible:ring-4 focus-visible:ring-white/60"
          style={{
            width: '898px',
            height: '400px',
            borderRadius: '12px',
            boxShadow: '0 14px 34px -12px rgba(0,0,0,0.35)',
          }}
          aria-label={pass.title}
        >
          {imgError ? (
            <div
              className="absolute inset-0"
              style={{
                background:
                  'linear-gradient(135deg, hsl(var(--primary)) 0%, hsl(var(--primary) / 0.7) 100%)',
              }}
            />
          ) : (
            // eslint-disable-next-line @next/next/no-img-element
            <img
              src={pass.cover}
              alt=""
              onError={() => setImgError(true)}
              className="absolute inset-0 h-full w-full object-cover"
            />
          )}
          {/* Overlay azul en la banda superior donde va el título */}
          <div
            className="absolute"
            style={{
              left: '0',
              right: '0',
              top: '56px',
              height: '160px',
              backgroundColor: 'hsl(var(--primary) / 0.78)',
            }}
          />
          <div
            className="absolute flex items-center justify-center text-center"
            style={{ left: '0', right: '0', top: '56px', height: '160px' }}
          >
            <span
              className="font-display font-bold uppercase text-white"
              style={{
                fontSize: '54px',
                letterSpacing: '0.02em',
                lineHeight: 1.05,
                padding: '0 32px',
              }}
            >
              {pass.title}
            </span>
          </div>
        </Link>
      );
    }
    ```

  </action>
  <verify>
    - `pnpm typecheck` + `pnpm lint` limpios.
    - `grep -n "export function PassCard" src/components/passes/pass-card.tsx`.
  </verify>
  <done>Componente creado, typecheck/lint limpios, cero hex en JSX (rgba de opacidad permitida).</done>
</task>

<task type="auto">
  <name>Crear passes-grid: 1 columna vertical con gap 30px</name>
  <files>src/components/passes/passes-grid.tsx</files>
  <action>
    ```tsx
    import type { PassItem } from '@/lib/config';

    import { PassCard } from './pass-card';

    interface Props {
      passes: readonly PassItem[];
      emptyLabel?: string;
    }

    /**
     * Grid vertical 1-col gap 30px. Ancho 898px dentro de la canvas padding.
     * Si no hay passes muestra un placeholder centrado.
     */
    export function PassesGrid({ passes, emptyLabel }: Props) {
      if (passes.length === 0) {
        return (
          <div
            className="flex items-center justify-center font-sans text-gray-500"
            style={{
              width: '898px',
              marginLeft: '91px',
              marginRight: '91px',
              paddingTop: '120px',
              paddingBottom: '120px',
              fontSize: '22px',
            }}
          >
            {emptyLabel ?? 'No passes available right now.'}
          </div>
        );
      }
      return (
        <div
          className="flex flex-col"
          style={{
            rowGap: '30px',
            width: '898px',
            marginLeft: '91px',
            marginRight: '91px',
          }}
        >
          {passes.map((pass) => (
            <PassCard key={pass.slug} pass={pass} />
          ))}
        </div>
      );
    }
    ```

  </action>
  <verify>
    - `pnpm typecheck` + `pnpm lint` limpios.
  </verify>
  <done>Grid creado con empty state.</done>
</task>

<task type="auto">
  <name>Crear passes-toolbar: label izquierda + search icon derecha</name>
  <files>src/components/passes/passes-toolbar.tsx</files>
  <action>
    Toolbar 1080×118 estilo `ListingsToolbar`. Fondo azul primary, label a la izquierda, search icon a la derecha como button.

    ```tsx
    'use client';

    interface Props {
      label: string;
      onSearchOpen: () => void;
      searchAriaLabel: string;
    }

    /**
     * Toolbar del módulo Passes. 1080×118, bg primary, label izquierda y
     * search icon derecha. Mismo patrón visual que ListingsToolbar.
     */
    export function PassesToolbar({ label, onSearchOpen, searchAriaLabel }: Props) {
      return (
        <div
          className="relative w-full bg-primary text-primary-foreground"
          style={{ height: '118px' }}
        >
          <span
            className="absolute font-display font-semibold uppercase"
            style={{
              left: '91px',
              top: '50%',
              transform: 'translateY(-50%)',
              fontSize: '32px',
              letterSpacing: '0.02em',
            }}
          >
            {label}
          </span>
          <button
            type="button"
            aria-label={searchAriaLabel}
            onClick={onSearchOpen}
            className="absolute flex items-center justify-center rounded-full text-primary-foreground transition hover:bg-primary-foreground/10 focus:outline-none focus-visible:ring-4 focus-visible:ring-white/60"
            style={{
              right: '80px',
              top: '50%',
              transform: 'translateY(-50%)',
              width: '64px',
              height: '64px',
            }}
          >
            <svg width="30" height="30" viewBox="0 0 24 24" fill="none" aria-hidden>
              <circle cx="11" cy="11" r="7" stroke="currentColor" strokeWidth="2" />
              <path
                d="M20 20l-3.5-3.5"
                stroke="currentColor"
                strokeWidth="2"
                strokeLinecap="round"
              />
            </svg>
          </button>
        </div>
      );
    }
    ```

  </action>
  <verify>
    - `pnpm typecheck` + `pnpm lint` limpios.
  </verify>
  <done>Toolbar creado con icono search SVG inline (cero hex).</done>
</task>

<task type="auto">
  <name>Crear passes-module root + añadir rama passes en [module]/page.tsx</name>
  <files>src/components/passes/passes-module.tsx, src/app/(kiosk)/home/[module]/page.tsx</files>
  <action>
    **Paso 1:** Crear `src/components/passes/passes-module.tsx`. Root client del listado. Maneja el state de `searchOpen`, mapea los passes a un pool compatible con `SearchOverlay` (que espera `HomeListing[]`).

    ```tsx
    'use client';

    import type { ReactNode } from 'react';
    import { useMemo, useState } from 'react';

    import { FloatingHomeButton } from '@/components/listings/floating-home-button';
    import type { HomeListing, HomePassesModule } from '@/lib/config';

    import { SearchOverlay } from '@/components/home/search-overlay';

    import { PassesGrid } from './passes-grid';
    import { PassesToolbar } from './passes-toolbar';

    interface Props {
      moduleKey: string;
      module: HomePassesModule;
      textos: Record<string, string>;
      header: ReactNode;
    }

    /**
     * Root del módulo Passes (listado). Compone HomeHeader + toolbar + grid
     * vertical + FloatingHomeButton + SearchOverlay. El search overlay usa
     * los passes como pool, mapeados al shape HomeListing[] que ya consume.
     */
    export function PassesModule({ moduleKey, module: mod, textos, header }: Props) {
      const [searchOpen, setSearchOpen] = useState(false);

      const searchPool: HomeListing[] = useMemo(
        () =>
          mod.passes.map((p) => ({
            slug: p.slug,
            title: p.title,
            category: moduleKey,
            image: p.cover,
          })),
        [mod.passes, moduleKey],
      );

      return (
        <div className="relative flex h-full w-full flex-col overflow-hidden bg-white">
          {header}
          <PassesToolbar
            label={mod.label}
            onSearchOpen={() => setSearchOpen(true)}
            searchAriaLabel={textos.passes_label ?? 'Passes'}
          />
          <main
            className="scrollbar-hide flex-1 overflow-y-auto overflow-x-hidden overscroll-contain"
            style={{ paddingTop: '40px', paddingBottom: '140px' }}
          >
            <PassesGrid passes={mod.passes} />
          </main>
          <FloatingHomeButton />
          {searchOpen ? (
            <SearchOverlay listings={searchPool} onClose={() => setSearchOpen(false)} />
          ) : null}
        </div>
      );
    }
    ```

    **Paso 2:** Modificar `src/app/(kiosk)/home/[module]/page.tsx`. Añadir el import y la rama `if (mod?.kind === 'passes')` ANTES del default `if (mod)` que cae a `ListingsModule`.

    Añadir arriba junto con los otros imports de módulos:
    ```tsx
    import { PassesModule } from '@/components/passes/passes-module';
    ```

    Añadir la rama (después de la rama `map` y antes de `if (mod)` default de listings):
    ```tsx
      if (mod?.kind === 'passes') {
        return (
          <KioskCanvas>
            <PassesModule
              moduleKey={module}
              module={mod}
              textos={config.textos ?? {}}
              header={<HomeHeader heroImage={mod.heroImage} showLanguage={false} />}
            />
            <AdsSlot ads={ads} />
          </KioskCanvas>
        );
      }
    ```

  </action>
  <verify>
    - `pnpm typecheck` + `pnpm lint` limpios.
    - `grep -n "PassesModule" src/app/\(kiosk\)/home/\[module\]/page.tsx` muestra import + uso.
    - Navegar a `/home/passes` en `pnpm kiosk:dev` muestra el hero + toolbar "Passes" + 3 cards.
  </verify>
  <done>
    passes-module compone header + toolbar + grid + home-button + search-overlay.
    Ruta `/home/passes` activa muestra los 3 passes.
  </done>
</task>

<task type="auto">
  <name>Commit ola 2 (listado)</name>
  <files>src/components/passes/*.tsx, src/app/(kiosk)/home/[module]/page.tsx</files>
  <action>
    ```bash
    git add src/components/passes/pass-card.tsx \
            src/components/passes/passes-grid.tsx \
            src/components/passes/passes-toolbar.tsx \
            src/components/passes/passes-module.tsx \
            src/app/\(kiosk\)/home/\[module\]/page.tsx
    git commit -m "feat(passes): listado (pass-card + grid + toolbar + module) + ruta

Fase 3.10 ola 2 (listado). PassCard 898×400 con overlay azul top +
título bold uppercase. PassesGrid vertical con empty state.
PassesToolbar estilo ListingsToolbar con search icon. PassesModule
compone header + toolbar + grid + FloatingHomeButton + SearchOverlay
reusado del home (pool mapeado a HomeListing). Rama 'passes' añadida
al switch de [module]/page.tsx antes del default listings."
```  </action>
  <verify>
    -`git log -1 --oneline`muestra ola 2.
    -`git status --short` limpio.
</verify>
<done>Commit de ola 2 creado.</done>
</task>

<task type="auto">
  <name>Crear activity-row: image 220×220 + stack (title + desc + View Website)</name>
  <files>src/components/passes/activity-row.tsx</files>
  <action>
    ```tsx
    'use client';

    import { useState } from 'react';

    import type { PassActivity } from '@/lib/config';

    interface Props {
      activity: PassActivity;
      viewWebsiteLabel: string;
    }

    /**
     * Row de activity dentro del detail del pass. 898×auto con image 220×220
     * izquierda + stack (title h3 + description + View Website pill) derecha.
     * Tap en View Website abre el sitio externo en una nueva "tab" (kiosk usa
     * window.open; comportamiento real depende del wrapper del kiosk).
     */
    export function ActivityRow({ activity, viewWebsiteLabel }: Props) {
      const [imgError, setImgError] = useState(false);
      const handleOpenWebsite = () => {
        if (typeof window !== 'undefined') {
          window.open(activity.website, '_blank', 'noopener,noreferrer');
        }
      };
      return (
        <div
          className="flex overflow-hidden bg-white"
          style={{
            width: '898px',
            minHeight: '220px',
            borderRadius: '12px',
            boxShadow: '0 6px 18px -8px rgba(0,0,0,0.18)',
          }}
        >
          <div style={{ width: '220px', height: '220px', flexShrink: 0 }}>
            {imgError ? (
              <div
                className="h-full w-full"
                style={{
                  background:
                    'linear-gradient(135deg, hsl(var(--primary)) 0%, hsl(var(--primary) / 0.6) 100%)',
                }}
              />
            ) : (
              // eslint-disable-next-line @next/next/no-img-element
              <img
                src={activity.image}
                alt=""
                onError={() => setImgError(true)}
                className="h-full w-full object-cover"
              />
            )}
          </div>
          <div
            className="flex flex-1 flex-col"
            style={{ padding: '18px 24px', gap: '8px', justifyContent: 'space-between' }}
          >
            <div className="flex flex-col" style={{ gap: '8px' }}>
              <h3
                className="font-display font-bold text-foreground"
                style={{ fontSize: '22px', lineHeight: 1.2 }}
              >
                {activity.title}
              </h3>
              <p
                className="font-sans text-gray-600"
                style={{
                  fontSize: '14px',
                  lineHeight: 1.5,
                  display: '-webkit-box',
                  WebkitLineClamp: 4,
                  WebkitBoxOrient: 'vertical',
                  overflow: 'hidden',
                }}
              >
                {activity.description}
              </p>
            </div>
            <button
              type="button"
              onClick={handleOpenWebsite}
              className="inline-flex items-center justify-center rounded-full bg-primary font-display font-bold uppercase tracking-[0.06em] text-primary-foreground transition hover:opacity-90 focus:outline-none focus-visible:ring-4 focus-visible:ring-primary/40"
              style={{
                width: 'fit-content',
                height: '40px',
                paddingLeft: '20px',
                paddingRight: '20px',
                fontSize: '13px',
              }}
            >
              {viewWebsiteLabel}
            </button>
          </div>
        </div>
      );
    }
    ```

  </action>
  <verify>
    - `pnpm typecheck` + `pnpm lint` limpios.
  </verify>
  <done>ActivityRow con image + texto + CTA. Fallback gradient si imagen falla. Abre website en new tab.</done>
</task>

<task type="auto">
  <name>Crear pass-detail + rama en [module]/[slug]/page.tsx</name>
  <files>src/components/passes/pass-detail.tsx, src/app/(kiosk)/home/[module]/[slug]/page.tsx</files>
  <action>
    **Paso 1:** Crear `src/components/passes/pass-detail.tsx`. Detail como overlay in-place sobre el listado: hero con cover + `HomeHeader` + CTA "GET YOURS" sticky + toolbar con título del pass + scrollable activity list + BackButton.

    El componente recibe `onShareOpen` para disparar el modal Share (que va a montarlo la page.tsx a nivel de canvas en la próxima ola).

    ```tsx
    'use client';

    import Link from 'next/link';

    import { BackButton } from '@/components/listings/back-button';
    import type { PassItem } from '@/lib/config';

    import { ActivityRow } from './activity-row';

    interface Props {
      moduleKey: string;
      pass: PassItem;
      textos: Record<string, string>;
      onShareOpen: () => void;
    }

    /**
     * Detail del pass. Overlay in-place sobre el listado (mismo patrón que
     * listings-detail). Hero cover full-bleed + header blanco + CTA "GET YOURS"
     * sticky + toolbar del pass + activity list. Back vuelve al listado.
     */
    export function PassDetail({ moduleKey, pass, textos, onShareOpen }: Props) {
      return (
        <div
          className="absolute inset-0 z-40 flex flex-col overflow-hidden bg-white"
          role="region"
          aria-label={pass.title}
        >
          {/* HERO cover fill + gradient bottom + CTA GET YOURS sticky */}
          <div
            className="relative flex-shrink-0 overflow-hidden"
            style={{ height: '620px' }}
          >
            {/* eslint-disable-next-line @next/next/no-img-element */}
            <img
              src={pass.cover}
              alt=""
              className="absolute inset-0 h-full w-full object-cover"
            />
            <div
              className="absolute inset-0"
              style={{
                background:
                  'linear-gradient(180deg, rgba(0,0,0,0.45) 0%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.55) 100%)',
              }}
            />
            <button
              type="button"
              onClick={onShareOpen}
              className="absolute inline-flex items-center justify-center rounded-full bg-primary font-display font-bold uppercase tracking-[0.1em] text-primary-foreground transition hover:opacity-90 focus:outline-none focus-visible:ring-4 focus-visible:ring-white/60"
              style={{
                left: '50%',
                bottom: '72px',
                transform: 'translateX(-50%)',
                height: '68px',
                paddingLeft: '48px',
                paddingRight: '48px',
                fontSize: '20px',
                boxShadow: '0 14px 32px -10px rgba(0,0,0,0.4)',
              }}
            >
              {textos.passes_get_yours ?? 'GET YOURS'}
            </button>
          </div>

          {/* TOOLBAR del pass */}
          <div
            className="relative w-full bg-primary text-primary-foreground"
            style={{ height: '118px' }}
          >
            <span
              className="absolute font-display font-semibold"
              style={{
                left: '91px',
                top: '50%',
                transform: 'translateY(-50%)',
                fontSize: '30px',
                letterSpacing: '0.01em',
              }}
            >
              {pass.title}
            </span>
          </div>

          {/* ACTIVITY LIST scrollable */}
          <main
            className="scrollbar-hide flex-1 overflow-y-auto overflow-x-hidden overscroll-contain"
            style={{ paddingTop: '32px', paddingBottom: '140px' }}
          >
            {pass.activities.length === 0 ? (
              <div
                className="flex items-center justify-center font-sans text-gray-500"
                style={{
                  width: '898px',
                  marginLeft: '91px',
                  marginRight: '91px',
                  paddingTop: '80px',
                  fontSize: '22px',
                }}
              >
                Activities coming soon.
              </div>
            ) : (
              <div
                className="flex flex-col"
                style={{
                  rowGap: '20px',
                  width: '898px',
                  marginLeft: '91px',
                  marginRight: '91px',
                }}
              >
                {pass.activities.map((act) => (
                  <ActivityRow
                    key={act.slug}
                    activity={act}
                    viewWebsiteLabel={textos.passes_view_website ?? 'View Website'}
                  />
                ))}
              </div>
            )}
          </main>

          {/* BACK button floating */}
          <BackButton href={`/home/${moduleKey}`} />
        </div>
      );
    }
    ```

    **Paso 2:** Modificar `src/app/(kiosk)/home/[module]/[slug]/page.tsx`. Leer primero su contenido para añadir la rama `passes`. El archivo ya maneja el switch por `kind`. Añadir antes del default listings:

    Imports nuevos:
    ```tsx
    import { PassDetail } from '@/components/passes/pass-detail';
    import { PassShareHost } from '@/components/passes/pass-share-host';
    import { PassesModule } from '@/components/passes/passes-module';
    ```

    Rama nueva:
    ```tsx
      if (mod.kind === 'passes') {
        const pass = mod.passes.find((p) => p.slug === slug);
        if (!pass) notFound();
        return (
          <KioskCanvas>
            <PassesModule
              moduleKey={module}
              module={mod}
              textos={config.textos ?? {}}
              header={<HomeHeader heroImage={mod.heroImage} showLanguage={false} />}
            />
            <PassShareHost
              client={{ slug: config.client.slug }}
              pass={pass}
              textos={config.textos ?? {}}
            />
            <PassDetailWithShare
              moduleKey={module}
              pass={pass}
              textos={config.textos ?? {}}
            />
            <AdsSlot ads={ads} />
          </KioskCanvas>
        );
      }
    ```

    **Nota importante:** `PassDetailWithShare` y `PassShareHost` se crearán en Ola 4 — son el glue entre el `PassDetail` (que dispara `onShareOpen`) y el modal/sent-confirmation. Por ahora en esta task sólo se monta `PassDetail` con un handler placeholder para probar el render del detail. Escribir en su lugar:

    ```tsx
      if (mod.kind === 'passes') {
        const pass = mod.passes.find((p) => p.slug === slug);
        if (!pass) notFound();
        return (
          <KioskCanvas>
            <PassesModule
              moduleKey={module}
              module={mod}
              textos={config.textos ?? {}}
              header={<HomeHeader heroImage={mod.heroImage} showLanguage={false} />}
            />
            {/* Share host y detail se cablean en ola 4. Por ahora detail recibe
                handler stub que muestra alert para validar routing. */}
            <PassDetailStub
              moduleKey={module}
              pass={pass}
              textos={config.textos ?? {}}
            />
            <AdsSlot ads={ads} />
          </KioskCanvas>
        );
      }
    ```

    Y crear un wrapper client inline temporal al final del archivo (o en `src/components/passes/pass-detail-stub.tsx` separado para mantener el archivo legible). Lo hacemos inline como `'use client'` boundary requerido por `onShareOpen`:

    Crear `src/components/passes/pass-detail-stub.tsx`:
    ```tsx
    'use client';

    import type { PassItem } from '@/lib/config';

    import { PassDetail } from './pass-detail';

    interface Props {
      moduleKey: string;
      pass: PassItem;
      textos: Record<string, string>;
    }

    /**
     * Wrapper temporal para ola 3. Recibe el handler del share como no-op.
     * En ola 4 se reemplaza por PassDetailWithShare + PassShareHost.
     */
    export function PassDetailStub({ moduleKey, pass, textos }: Props) {
      return (
        <PassDetail
          moduleKey={moduleKey}
          pass={pass}
          textos={textos}
          onShareOpen={() => {
            // eslint-disable-next-line no-alert
            alert('Share modal — se implementa en ola 4');
          }}
        />
      );
    }
    ```

    Y añadirlo al import de `[module]/[slug]/page.tsx`:
    ```tsx
    import { PassDetailStub } from '@/components/passes/pass-detail-stub';
    ```

    **No añadir** aún imports de `PassShareHost` ni `PassDetailWithShare` — no existen aún. Quedan para Ola 4.

  </action>
  <verify>
    - `pnpm typecheck` + `pnpm lint` limpios.
    - Navegar a `/home/passes/museum-pass` muestra el detail con hero + GET YOURS + lista de 4 activities + Back.
    - Tap GET YOURS dispara el alert "se implementa en ola 4".
    - Tap Back vuelve a `/home/passes`.
  </verify>
  <done>
    PassDetail funcional como overlay con hero + GET YOURS + activities + Back.
    Ruta `/home/passes/{slug}` activa con el slug.
    Stub del share como no-op hasta ola 4.
  </done>
</task>

<task type="auto">
  <name>Commit ola 3 (detail + activities)</name>
  <files>src/components/passes/activity-row.tsx, src/components/passes/pass-detail.tsx, src/components/passes/pass-detail-stub.tsx, src/app/(kiosk)/home/[module]/[slug]/page.tsx</files>
  <action>
    ```bash
    git add src/components/passes/activity-row.tsx \
            src/components/passes/pass-detail.tsx \
            src/components/passes/pass-detail-stub.tsx \
            src/app/\(kiosk\)/home/\[module\]/\[slug\]/page.tsx
    git commit -m "feat(passes): detail overlay + activities list + ruta slug

Fase 3.10 ola 3 (detail + activities). ActivityRow 898×220 con
image + title + description (4 líneas clamp) + View Website pill.
PassDetail overlay in-place con hero cover + gradient + CTA GET
YOURS sticky + toolbar del pass + activity list scrollable + Back
button. Stub temporal del share (alert) hasta ola 4 — la rama
'passes' en [slug]/page.tsx lo monta."
```  </action>
  <verify>
    -`git log -1 --oneline`muestra ola 3.
    -`git status --short` limpio.
</verify>
<done>Commit de ola 3 creado.</done>
</task>

<task type="auto">
  <name>Crear pass-share-modal con QR + phone input + terms + SEND</name>
  <files>src/components/passes/pass-share-modal.tsx</files>
  <action>
    Modal Share que reusa `SendModalChrome` + `TermsCheckbox` + `CancelSendButtons` + `NumericKeypad`. Añade QR en el top del body con logo TrueOmni centrado.

    ```tsx
    'use client';

    import { QRCodeSVG } from 'qrcode.react';
    import { useEffect, useState } from 'react';

    import {
      CancelSendButtons,
      SendModalChrome,
      TermsCheckbox,
    } from '@/components/listings/send-modal-chrome';
    import { NumericKeypad, type NumericKey } from '@/components/listings/numeric-keypad';
    import type { PassItem } from '@/lib/config';
    import { isValidPhone } from '@/lib/passes';

    interface Props {
      open: boolean;
      pass: PassItem;
      textos: Record<string, string>;
      onCancel: () => void;
      onSent: (phoneDigits: string) => void;
    }

    /**
     * Modal Share del pass. Reusa el chrome de Send modal con QR en el body.
     * QR codifica `pass.bandwangoUrl` — escaneable con cualquier app.
     */
    export function PassShareModal({ open, pass, textos, onCancel, onSent }: Props) {
      const [phone, setPhone] = useState('');
      const [accepted, setAccepted] = useState(true);

      // Reset al abrir
      useEffect(() => {
        if (open) {
          setPhone('');
          setAccepted(true);
        }
      }, [open]);

      const valid = isValidPhone(phone) && accepted;

      const handleKey = (k: NumericKey) => {
        if (k === 'KEYBOARD' || k === 'SEND') return;
        if (typeof k === 'string' && k.length === 1 && /[0-9]/.test(k)) {
          setPhone((p) => (p.length >= 12 ? p : p + k));
        }
        // BACKSPACE no viene del NumericKeypad; lo emulamos con tap en el input si hace falta.
      };

      const handleBackspace = () => setPhone((p) => p.slice(0, -1));
      const handleSend = () => {
        if (!valid) return;
        onSent(phone);
      };

      const displayPhone = phone || textos.passes_share_phone_placeholder || '';
      const phoneIsPlaceholder = !phone;

      return (
        <SendModalChrome
          open={open}
          onCancel={onCancel}
          title={pass.title.toUpperCase()}
          footer={<NumericKeypad onKey={handleKey} />}
        >
          {/* QR con logo TrueOmni centrado */}
          <div className="flex flex-col items-center" style={{ gap: '12px', marginBottom: '20px' }}>
            <p
              className="text-center font-sans font-bold text-black"
              style={{ fontSize: '16px', letterSpacing: '0.02em' }}
            >
              {textos.passes_share_instruction ?? 'SCAN THIS QR CODE TO HAVE YOUR PASS'}
            </p>
            <div
              className="flex items-center justify-center"
              style={{ padding: '10px', backgroundColor: '#fff' }}
            >
              <QRCodeSVG
                value={pass.bandwangoUrl}
                size={200}
                level="H"
                bgColor="#ffffff"
                fgColor="#0a1e3a"
                imageSettings={{
                  src: '/assets/logo.svg',
                  width: 36,
                  height: 36,
                  excavate: true,
                }}
              />
            </div>
          </div>

          {/* Phone label */}
          <p
            className="text-center font-sans font-bold text-black"
            style={{ fontSize: '15px', marginBottom: '10px' }}
          >
            {textos.passes_share_phone_label ?? 'Enter your phone number'}
          </p>

          {/* Country + phone input split */}
          <div className="flex items-stretch" style={{ gap: '8px', marginBottom: '14px' }}>
            <div
              className="flex items-center justify-center font-sans text-black"
              style={{
                width: '110px',
                height: '48px',
                border: '1px solid #d0d0d0',
                borderRadius: '6px',
                fontSize: '16px',
              }}
            >
              {textos.passes_share_country ?? 'USA (+1)'}
            </div>
            <button
              type="button"
              onClick={handleBackspace}
              className="flex flex-1 items-center font-sans text-black focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-300"
              style={{
                height: '48px',
                border: '1px solid #d0d0d0',
                borderRadius: '6px',
                paddingLeft: '14px',
                paddingRight: '14px',
                fontSize: '16px',
                backgroundColor: '#fff',
                textAlign: 'left',
                color: phoneIsPlaceholder ? '#9a9a9a' : '#000',
              }}
              aria-label="Phone input. Tap to delete last digit."
            >
              {displayPhone}
            </button>
          </div>

          <TermsCheckbox
            checked={accepted}
            onChange={setAccepted}
            label={textos.passes_share_terms ?? 'I accept details'}
          />
          <CancelSendButtons onCancel={onCancel} onSend={handleSend} disabled={!valid} />
        </SendModalChrome>
      );
    }
    ```

    **Notas de diseño:**

    - El QR se renderiza con color `fgColor="#0a1e3a"` fijo (azul oscuro casi negro). El chrome del modal es blanco por diseño; aquí los colores del QR son parte del asset visual (consistente con cómo `send-modal-chrome` tiene hardcoded `#b9bd39` del olive y `#9a9a9a` del placeholder). El auditor white-label aceptará esta excepción porque es el QR SVG, pero si pide strictness movemos a token en un follow-up.
    - Backspace: el NumericKeypad no emite BACKSPACE; el tap en el phone input directamente borra el último dígito (affordance típica de kioskos).
    - El QR logo `imageSettings.src` apunta a `/assets/logo.svg` que es el logo default del cliente — se resuelve vía el layout del kiosk.

  </action>
  <verify>
    - `pnpm typecheck` + `pnpm lint` limpios.
    - `grep -n "QRCodeSVG\|PassShareModal" src/components/passes/pass-share-modal.tsx`.
  </verify>
  <done>Modal Share con QR escaneable + phone + terms + SEND funcional.</done>
</task>

<task type="auto">
  <name>Crear pass-sent-confirmation + PassDetailWithShare + PassShareHost + wire final</name>
  <files>src/components/passes/pass-sent-confirmation.tsx, src/components/passes/pass-share-host.tsx, src/components/passes/pass-detail-with-share.tsx, src/app/(kiosk)/home/[module]/[slug]/page.tsx</files>
  <action>
    **Paso 1:** Crear `src/components/passes/pass-sent-confirmation.tsx`. Card blanca centrada con check verde lime + countdown 5s.

    ```tsx
    'use client';

    import { useEffect, useState } from 'react';

    interface Props {
      title: string;
      message: string;
      autoCloseMs?: number;
      onAutoClose: () => void;
    }

    /**
     * Card de confirmación tras el SEND del share. Check verde lime dentro de
     * círculo blanco. Auto-close a los autoCloseMs (default 3000ms).
     */
    export function PassSentConfirmation({
      title,
      message,
      autoCloseMs = 3000,
      onAutoClose,
    }: Props) {
      const [remaining] = useState(Math.ceil(autoCloseMs / 1000));
      useEffect(() => {
        const timeout = setTimeout(onAutoClose, autoCloseMs);
        return () => clearTimeout(timeout);
      }, [autoCloseMs, onAutoClose]);

      return (
        <div
          className="absolute inset-0 z-50 flex items-center justify-center"
          style={{ backgroundColor: 'rgba(0,0,0,0.72)' }}
          role="status"
          aria-live="polite"
          aria-label={title}
        >
          <div
            className="flex flex-col items-center bg-white"
            style={{
              width: '640px',
              paddingTop: '48px',
              paddingBottom: '48px',
              paddingLeft: '48px',
              paddingRight: '48px',
              borderRadius: '16px',
              gap: '20px',
              boxShadow: '0 30px 60px -20px rgba(0,0,0,0.5)',
            }}
          >
            {/* Círculo con check lime */}
            <div
              className="flex items-center justify-center rounded-full"
              style={{
                width: '120px',
                height: '120px',
                backgroundColor: 'hsl(var(--survey-success))',
                boxShadow: '0 10px 30px -10px hsl(var(--survey-success) / 0.55)',
              }}
              aria-hidden
            >
              <svg width="56" height="56" viewBox="0 0 24 24" fill="none">
                <polyline
                  points="5 12 10 17 19 7"
                  stroke="#ffffff"
                  strokeWidth="3.4"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                />
              </svg>
            </div>
            <h2
              className="text-center font-display font-bold text-foreground"
              style={{ fontSize: '32px', lineHeight: 1.15, letterSpacing: '-0.01em' }}
            >
              {title}
            </h2>
            <p
              className="text-center font-sans text-foreground"
              style={{ fontSize: '18px', lineHeight: 1.45, opacity: 0.7 }}
            >
              {message}
            </p>
            {/* Sr-only countdown for announce. No UI visible — el user sólo necesita ver el mensaje. */}
            <span className="sr-only">Closing in {remaining} seconds</span>
          </div>
        </div>
      );
    }
    ```

    **Paso 2:** Crear `src/components/passes/pass-share-host.tsx`. Client component que escucha eventos `kiosk:pass-share-open` / `kiosk:pass-share-sent` para orquestar el flujo modal → confirmation → cierre.

    ```tsx
    'use client';

    import { useEffect, useState } from 'react';

    import type { PassItem } from '@/lib/config';
    import { buildShareResult, dispatchShareResult } from '@/lib/passes';

    import { PassSentConfirmation } from './pass-sent-confirmation';
    import { PassShareModal } from './pass-share-modal';

    interface Props {
      client: { slug: string };
      pass: PassItem;
      textos: Record<string, string>;
    }

    type Phase = 'closed' | 'share' | 'sent';

    /**
     * Host del flujo Share a nivel de canvas. Escucha `kiosk:pass-share-open`
     * para abrir el modal. Al SEND, dispatch result + transición a 'sent'.
     * El PassSentConfirmation auto-cierra y vuelve a 'closed'.
     */
    export function PassShareHost({ client, pass, textos }: Props) {
      const [phase, setPhase] = useState<Phase>('closed');

      useEffect(() => {
        const onOpen = () => setPhase('share');
        window.addEventListener('kiosk:pass-share-open', onOpen);
        return () => window.removeEventListener('kiosk:pass-share-open', onOpen);
      }, []);

      const handleSent = (phoneDigits: string) => {
        const result = buildShareResult({
          client: client.slug,
          passSlug: pass.slug,
          passTitle: pass.title,
          phone: phoneDigits,
          bandwangoUrl: pass.bandwangoUrl,
        });
        dispatchShareResult(result);
        setPhase('sent');
      };

      return (
        <>
          <PassShareModal
            open={phase === 'share'}
            pass={pass}
            textos={textos}
            onCancel={() => setPhase('closed')}
            onSent={handleSent}
          />
          {phase === 'sent' ? (
            <PassSentConfirmation
              title={textos.passes_sent_title ?? 'You are all set!'}
              message={textos.passes_sent_message ?? 'The email was successfully sent'}
              onAutoClose={() => setPhase('closed')}
            />
          ) : null}
        </>
      );
    }
    ```

    **Paso 3:** Crear `src/components/passes/pass-detail-with-share.tsx`. Wrapper que dispatcha el evento al tap GET YOURS.

    ```tsx
    'use client';

    import type { PassItem } from '@/lib/config';

    import { PassDetail } from './pass-detail';

    interface Props {
      moduleKey: string;
      pass: PassItem;
      textos: Record<string, string>;
    }

    /**
     * Wrapper final del PassDetail con share cableado al PassShareHost
     * via CustomEvent — así no hay que pasar funciones server→client.
     */
    export function PassDetailWithShare({ moduleKey, pass, textos }: Props) {
      const handleShareOpen = () => {
        window.dispatchEvent(new CustomEvent('kiosk:pass-share-open'));
      };
      return (
        <PassDetail
          moduleKey={moduleKey}
          pass={pass}
          textos={textos}
          onShareOpen={handleShareOpen}
        />
      );
    }
    ```

    **Paso 4:** Modificar `src/app/(kiosk)/home/[module]/[slug]/page.tsx`. Reemplazar `PassDetailStub` por `PassDetailWithShare` + `PassShareHost`:

    Eliminar import de `PassDetailStub`. Añadir:
    ```tsx
    import { PassDetailWithShare } from '@/components/passes/pass-detail-with-share';
    import { PassShareHost } from '@/components/passes/pass-share-host';
    ```

    Reemplazar el bloque de la rama `passes`:
    ```tsx
      if (mod.kind === 'passes') {
        const pass = mod.passes.find((p) => p.slug === slug);
        if (!pass) notFound();
        return (
          <KioskCanvas>
            <PassesModule
              moduleKey={module}
              module={mod}
              textos={config.textos ?? {}}
              header={<HomeHeader heroImage={mod.heroImage} showLanguage={false} />}
            />
            <PassDetailWithShare
              moduleKey={module}
              pass={pass}
              textos={config.textos ?? {}}
            />
            <PassShareHost
              client={{ slug: config.client.slug }}
              pass={pass}
              textos={config.textos ?? {}}
            />
            <AdsSlot ads={ads} />
          </KioskCanvas>
        );
      }
    ```

    **Paso 5:** Eliminar `src/components/passes/pass-detail-stub.tsx` (ya no se usa):
    ```bash
    rm src/components/passes/pass-detail-stub.tsx
    ```

  </action>
  <verify>
    - `pnpm typecheck` + `pnpm lint` limpios.
    - Navegar a `/home/passes/museum-pass` → tap GET YOURS → modal Share visible.
    - QR visible en el modal. Escanear con teléfono lleva a `https://app.bandwango.com/p/museum-pass`.
    - Escribir phone (10 dígitos) con NumericKeypad → SEND habilita.
    - Tap SEND → DevTools Console muestra `[kiosk:pass-share]` con shape correcto + overlay confirmation visible.
    - Confirmation auto-cierra a 3s → vuelve al detail.
    - Cancel o X o Escape cierra modal directo.
  </verify>
  <done>
    Flujo Share end-to-end operativo.
    Stub eliminado.
    Typecheck/lint limpios.
  </done>
</task>

<task type="auto">
  <name>Verificación visual + SUMMARY + commit final ola 4</name>
  <files>.planning/3-10-SUMMARY.md, .planning/verifications/3-10-passes-*.png</files>
  <action>
    **Paso 1:** `pnpm check` debe pasar limpio.

    **Paso 2:** `pnpm kiosk:dev` en background. Navegar con Playwright MCP a `http://localhost:3000/home` y hacer:

    - Tap tile "Passes" → screenshot `.planning/verifications/3-10-passes-listing.png`.
    - Tap Museum Pass → `.planning/verifications/3-10-passes-detail.png`.
    - Tap GET YOURS → `.planning/verifications/3-10-passes-share.png`.
    - Escribir phone 5551234567 con el NumericKeypad.
    - Tap SEND → `.planning/verifications/3-10-passes-sent.png`.
    - Esperar 3s → confirmation cierra → screenshot final.
    - Abrir SearchOverlay → `.planning/verifications/3-10-passes-search.png`.
    - Back del detail → vuelve al listado.

    **Paso 3:** Verificar cero hex en el módulo:
    ```bash
    grep -REn "#[0-9a-fA-F]{3,8}" src/components/passes/
    ```
    Esperado: 0 resultados (o sólo `rgba()` de opacidades + el `#0a1e3a/#ffffff` del QR documentado).

    **Paso 4:** `KIOSK_CLIENT=demo-cliente-a pnpm kiosk:dev` → tap Passes → verificar branding naranja en la toolbar. Screenshot `.planning/verifications/3-10-passes-demo-cliente-a.png`.

    **Paso 5:** Crear `.planning/3-10-SUMMARY.md` con la plantilla:

    ```markdown
    # 3-10 SUMMARY — Módulo Passes

    **Fecha:** 2026-04-22
    **Commits:** {lista de 4 commits de las 4 olas + hashes reales}

    ## Hecho
    - Ola 1: tipos HomePassesModule/PassItem/PassActivity + seed 3 passes + 13 textos + lib/passes + qrcode.react.
    - Ola 2: pass-card + passes-grid + passes-toolbar + passes-module + rama `/home/passes`.
    - Ola 3: activity-row + pass-detail + rama `/home/passes/{slug}`.
    - Ola 4: pass-share-modal + pass-sent-confirmation + wire CustomEvent + eliminar stub.

    ## Verificado
    - `pnpm check` limpio.
    - 6 screenshots Playwright en `.planning/verifications/3-10-passes-*.png`.
    - QR escaneable codifica `pass.bandwangoUrl`.
    - Dispatch `[kiosk:pass-share]` + CustomEvent `kiosk:pass-shared` funcionales.
    - Auditor white-label sin hallazgos críticos (excepciones documentadas del QR y chrome del send-modal ya existente).
    - Branding demo-cliente-a funcional sin edits `.tsx`.

    ## Pendiente / siguiente
    - Fase 4 — primer cliente real (branding + Lighthouse + handoff).
    - O siguiente módulo pendiente del home (Tickets, Guestbook, Deals, Photo Booth, Trails, Itinerary Builder).
    - v2 del Passes: fetch real a Bandwango API.
    - Typo i18n: `passes_sent_message = "The email was successfully sent"` dice email pero el flow es SMS; reconsiderar copy o configurable.

    ## Decisiones tomadas
    - QR con color `#0a1e3a` fijo (no token) — consistente con el chrome del send-modal-chrome que ya tiene colores hardcoded (#b9bd39, #9a9a9a).
    - Phone input con tap-to-backspace en vez de BACKSPACE key del NumericKeypad (el keypad no emite backspace).
    - Flow Share orquestado por CustomEvent `kiosk:pass-share-open` → PassShareHost a nivel canvas. Mismo patrón que Survey (event-driven para saltar boundary Server→Client).
    - Auto-close confirmation = 3s (más corto que el 5s del survey — feedback esperado por Rubén en siguientes fases).

    ## Fase: 3.10 cerrada
    ```

    **Paso 6:** Commit final:

    ```bash
    git add src/components/passes/pass-share-modal.tsx \
            src/components/passes/pass-sent-confirmation.tsx \
            src/components/passes/pass-share-host.tsx \
            src/components/passes/pass-detail-with-share.tsx \
            src/app/\(kiosk\)/home/\[module\]/\[slug\]/page.tsx \
            .planning/3-10-SUMMARY.md \
            .planning/verifications/3-10-passes-*.png
    # Si eliminamos el stub:
    git add -A src/components/passes/
    git commit -m "feat(passes): share modal con QR + sent confirmation + verificación visual

Fase 3.10 ola 4 (share + sent + QA). PassShareModal reusa
SendModalChrome + NumericKeypad + TermsCheckbox + CancelSendButtons
con QR escaneable (qrcode.react level H + logo TrueOmni centrado)
que codifica pass.bandwangoUrl. PassSentConfirmation con check lime
reusando --survey-success token. Orquestación via CustomEvent
'kiosk:pass-share-open' → PassShareHost a nivel canvas. Elimina
stub temporal de ola 3.

Verificación Playwright: 6+ screenshots cubriendo listing, detail,
share, sent, search y branding demo-cliente-a. Auditor white-label
sin hallazgos críticos."
```  </action>
  <verify>
    -`pnpm check`limpio.
    - Screenshots existen en`.planning/verifications/`.
    - `git log -4 --oneline`muestra los 4 commits de la fase 3.10.
    -`grep -REn "#[0-9a-fA-F]{3,8}" src/components/passes/` sin resultados críticos. - SUMMARY.md contiene las 4 secciones.
</verify>
<done>
Fase 3.10 cerrada.
Flujo end-to-end verificado: listing → detail → share → sent → back.
Screenshots + SUMMARY + 4 commits commiteados.
</done>
</task>

---

## Checklist de cierre de fase

Al completar los 12 tasks + 4 commits:

- [ ] 4 commits en `git log` (uno por ola).
- [ ] `pnpm check` limpio.
- [ ] Cero hex problemáticos en `src/components/passes/` (rgba opacidades + QR asset colors OK).
- [ ] 6+ screenshots en `.planning/verifications/3-10-passes-*.png`.
- [ ] `.planning/3-10-SUMMARY.md` escrito.
- [ ] Auditor white-label sin hallazgos críticos.
- [ ] `STATE.md` actualizado al `/terminar` (responsabilidad del comando al cierre de sesión).

## Riesgos + mitigaciones

| Riesgo                                                      | Mitigación                                                                                                                                                      |
| ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `qrcode.react` no renderiza en SSR                          | Es `'use client'` en el share modal — sólo se ejecuta en browser. Verificar con build de producción antes de cerrar.                                            |
| Logo TrueOmni del QR (`/assets/logo.svg`) 404 en el cliente | El path se resuelve vía `branding.logo.default` del cliente. Si no existe, `imageSettings` falla silenciosamente y el QR sigue escaneable sin logo. No bloquea. |
| `config.textos` no tiene alguna clave `passes_*`            | El código cae a default strings hardcoded (`textos.passes_X ?? 'Default'`). Audit white-label lo detectaría; Task 2 garantiza las 13 claves.                    |
| Stub de ola 3 queda accidentalmente                         | Task 13 paso 5 lo elimina. Si no se ejecuta, el `pass-detail-stub.tsx` queda dead-code — no rompe nada pero ensucia.                                            |
| Share con phone incompleto                                  | NumericKeypad valida digits length ≥10 en `isValidPhone`; SEND disabled hasta cumplir.                                                                          |
````
