# Plan 3-11-1 — Módulo Tickets (filter over Events + buy via QR)

**Fase:** 3.11 · **Orden:** 1 · **Depende de:** fases 3.2-3.10 commiteadas.
**Cubre requerimientos:** R1 (pantallas), R2 (white-label), R7 (UX home), R8 (modales).
**Skills necesarios al ejecutar:** Tier 1 (`frontend-design`, `ui-ux-pro-max`, `theme-factory`). Tier 2 (`web-design-guidelines`) al cerrar.
**Spec de referencia:** `docs/superpowers/specs/2026-04-22-tickets-module-design.md` (commit `ad2a926`).

**Resumen:** Se implementa el módulo Tickets como nuevo discriminador `kind: 'tickets'`. Tickets ⊂ Events (subset filtrado por la presencia del campo opcional `ticket` en `EventItem`). Se refactoriza el flow QR + teléfono de Passes a un componente compartido `src/components/shared/qr-purchase-{modal,host}.tsx` reutilizable por Passes, Tickets y Events con ticket. Events con `.ticket` presente también dispara el QR popup (reemplaza `ticketsUrl` legacy cuando existe). Favoritos compartidos (`kiosk_event_favorites`). Seed: 10 events nuevos ticketables mezclados distribuidos en `modules.events.events[]` + bloque `modules.tickets` + tile nuevo. 20 tasks en 4 olas con 4 commits finales.

**Orden de ejecución:**

- Ola 1 (Foundations + refactor QR + seed): T1-T7. Tipos + mover modal a shared/ + Passes migration + seed + config.
- Ola 2 (Tickets listing): T8-T13. lib + card + list + filter overlay + module + ruta listado.
- Ola 3 (Detail + buy flow): T14-T18. ListingDetail onClick + detail-with-share + purchase host + ruta detail + Events dispatcha QR.
- Ola 4 (QA + cierre): T19-T20. Verificación Playwright + auditor white-label + SUMMARY.

---

## Ola 1 — Foundations + refactor QR + seed

<task type="auto">
  <name>T1: Añadir ticket? a EventItem, HomeTicketsModule, variant union</name>
  <files>src/lib/config.ts</files>
  <action>
    Abrir `src/lib/config.ts`. Localizar la `export interface EventItem` (alrededor de línea 154-183). Añadir al FINAL de la interface, antes del `}`:

    ```ts
      /** Si está presente, el evento vende boletos y aparece en el módulo Tickets. */
      ticket?: {
        /** Texto libre a mostrar en la card/detail: "$25", "$15–30", "From $10". */
        priceDisplay: string;
        /** URL absoluta que va codificada en el QR del popup de compra. */
        purchaseUrl: string;
      };
    ```

    Luego localizar `HomePassesModule` (alrededor de línea 340-346) y añadir INMEDIATAMENTE DESPUÉS:

    ```ts
    /** Módulo Tickets — kind discriminator 'tickets'. Vista filtrada sobre events ticketables. */
    export interface HomeTicketsModule {
      kind: 'tickets';
      label: string;
      heroImage: string;
      /** Opcionales — si faltan, el filter overlay deriva del pool visible. */
      categories?: string[];
      venues?: string[];
      features?: string[];
    }
    ```

    Luego localizar el union `export type HomeModuleVariant =` y añadir `| HomeTicketsModule` como última alternativa. Queda:

    ```ts
    export type HomeModuleVariant =
      | HomeModule
      | HomeEventsModule
      | HomeSocialWallModule
      | HomeDigitalBrochureModule
      | HomeMapModule
      | HomePassesModule
      | HomeTicketsModule;
    ```

    **No tocar** nada más en el archivo.
  </action>
  <verify>
    - `pnpm typecheck` pasa limpio.
    - `grep -n "HomeTicketsModule\|ticket?:" src/lib/config.ts` muestra 3 líneas mínimo (interface, union, campo opcional).
  </verify>
  <done>
    `ticket?` en `EventItem`. `HomeTicketsModule` interface exportada. Union actualizada. Typecheck limpio.
  </done>
</task>

<task type="auto">
  <name>T2: Crear src/components/shared/qr-purchase-modal.tsx (refactor desde passes)</name>
  <files>src/components/shared/qr-purchase-modal.tsx</files>
  <action>
    **Crear** el directorio `src/components/shared/` si no existe.

    **Crear** `src/components/shared/qr-purchase-modal.tsx` con el siguiente contenido (adaptado desde `src/components/passes/pass-share-modal.tsx` con props genéricas y opcional `priceDisplay`):

    ```tsx
    'use client';

    import { QRCodeSVG } from 'qrcode.react';
    import { useEffect, useState } from 'react';

    import { NumericKeypad, type NumericKey } from '@/components/listings/numeric-keypad';
    import { TermsCheckbox } from '@/components/listings/send-modal-chrome';
    import { useEscapeToClose } from '@/components/listings/use-escape-to-close';

    /** Valida: mínimo 10 dígitos numéricos. */
    function isValidPhone(digits: string): boolean {
      return digits.replace(/\D/g, '').length >= 10;
    }

    interface Props {
      open: boolean;
      /** Título superior del modal (ej. "MUSEUM PASS" o "SPRING HALF MARATHON"). */
      title: string;
      /** URL que se codifica en el QR y se envía por SMS. */
      purchaseUrl: string;
      /** Precio opcional visible encima del QR (solo Tickets; Passes no usa). */
      priceDisplay?: string;
      textos: Record<string, string>;
      /** Logo centrado en el QR. Si se omite, el QR no lleva imagen. */
      qrLogo?: string;
      onCancel: () => void;
      onSent: (phoneDigits: string) => void;
    }

    /**
     * Modal genérico de compra/share vía QR + SMS.
     *
     * Compartido entre Passes (Fase 3.10) y Tickets (Fase 3.11). Chrome propio
     * con header azul `#004f8b`, X circular blanca top-right, body blanco con
     * QR + phone input + SEND. NumericKeypad footer envuelto en wrapper blanco.
     *
     * Card 800×auto — alineado al SVG Passes-Share.png.
     */
    export function QrPurchaseModal({
      open,
      title,
      purchaseUrl,
      priceDisplay,
      textos,
      qrLogo,
      onCancel,
      onSent,
    }: Props) {
      const [phone, setPhone] = useState('');
      const [accepted, setAccepted] = useState(true);

      useEscapeToClose(open, onCancel);

      useEffect(() => {
        if (open) {
          setPhone('');
          setAccepted(true);
        }
      }, [open]);

      const valid = isValidPhone(phone) && accepted;

      const handleKey = (k: NumericKey) => {
        if (k === 'SEND') {
          if (valid) onSent(phone);
          return;
        }
        if (k === 'KEYBOARD') return;
        if (typeof k === 'string' && k.length === 1) {
          setPhone((p) => (p.length >= 14 ? p : p + k));
        }
      };

      const handleSend = () => {
        if (!valid) return;
        onSent(phone);
      };

      if (!open) return null;

      return (
        <div
          role="dialog"
          aria-modal="true"
          aria-label={title}
          className="absolute inset-0"
          style={{ zIndex: 40 }}
        >
          <button
            type="button"
            aria-label="Cerrar"
            onClick={onCancel}
            className="absolute inset-0 cursor-default focus:outline-none"
            style={{ backgroundColor: 'rgba(30,30,30,0.85)' }}
            tabIndex={-1}
          />

          <div
            className="absolute overflow-hidden bg-white"
            style={{
              left: '140px',
              top: '360px',
              width: '800px',
              borderRadius: '12px',
              boxShadow: '0 30px 50px rgba(0,0,0,0.4)',
            }}
          >
            <div
              className="relative flex items-center justify-center"
              style={{ height: '92px', backgroundColor: '#004f8b' }}
            >
              <h2
                className="font-sans font-bold uppercase text-white"
                style={{ fontSize: '30px', letterSpacing: '0.04em' }}
              >
                {title}
              </h2>
              <button
                type="button"
                onClick={onCancel}
                aria-label="Cerrar"
                className="absolute flex items-center justify-center rounded-full focus:outline-none focus-visible:ring-4 focus-visible:ring-white/60"
                style={{
                  right: '24px',
                  top: '50%',
                  transform: 'translateY(-50%)',
                  width: '48px',
                  height: '48px',
                  border: '2.5px solid #fff',
                }}
              >
                <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden>
                  <path
                    d="M5 5l10 10M15 5L5 15"
                    stroke="#fff"
                    strokeWidth="2.6"
                    strokeLinecap="round"
                  />
                </svg>
              </button>
            </div>

            <div style={{ padding: '32px 56px 36px 56px' }}>
              <p
                className="text-center font-sans font-bold text-black"
                style={{ fontSize: '20px', letterSpacing: '0.04em', marginBottom: '20px', lineHeight: 1.25 }}
              >
                {textos.qr_instruction ?? 'SCAN QR OR GET IT BY SMS'}
              </p>

              {priceDisplay ? (
                <p
                  className="text-center font-display font-bold"
                  style={{ fontSize: '34px', color: '#004f8b', marginBottom: '16px', letterSpacing: '0.02em' }}
                >
                  {priceDisplay}
                </p>
              ) : null}

              <div className="flex items-center justify-center bg-white" style={{ marginBottom: '24px' }}>
                <QRCodeSVG
                  value={purchaseUrl}
                  size={260}
                  level="H"
                  bgColor="#ffffff"
                  fgColor="#0a1e3a"
                  imageSettings={qrLogo ? { src: qrLogo, width: 48, height: 48, excavate: true } : undefined}
                />
              </div>

              <p className="text-center font-sans font-bold text-black" style={{ fontSize: '18px', marginBottom: '14px' }}>
                {textos.qr_phone_label ?? 'Enter your phone number'}
              </p>

              <div className="flex items-stretch" style={{ columnGap: '14px', marginBottom: '18px' }}>
                <div
                  className="flex items-center justify-center font-sans text-black"
                  style={{
                    width: '140px',
                    height: '56px',
                    border: '1px solid #bfbfbf',
                    borderRadius: '6px',
                    fontSize: '18px',
                  }}
                >
                  {textos.qr_country ?? 'USA (+1)'}
                </div>
                <input
                  type="tel"
                  value={phone}
                  readOnly
                  placeholder={textos.qr_phone_placeholder ?? '000-555-0115'}
                  aria-label={textos.qr_phone_aria ?? 'Phone number. Tap to edit via keypad.'}
                  className="flex-1 font-sans"
                  style={{
                    height: '56px',
                    border: '1px solid #bfbfbf',
                    borderRadius: '6px',
                    padding: '0 18px',
                    fontSize: '18px',
                    color: '#333',
                  }}
                />
              </div>

              <div className="flex justify-center" style={{ marginBottom: '22px' }}>
                <TermsCheckbox
                  checked={accepted}
                  onChange={setAccepted}
                  label={textos.qr_terms ?? 'I accept details'}
                />
              </div>

              <div className="flex justify-center">
                <button
                  type="button"
                  onClick={handleSend}
                  disabled={!valid}
                  className="font-sans font-bold uppercase text-white focus:outline-none focus-visible:ring-4 focus-visible:ring-blue-300"
                  style={{
                    width: '320px',
                    height: '68px',
                    borderRadius: '8px',
                    backgroundColor: valid ? '#1796d6' : 'rgba(23,150,214,0.5)',
                    fontSize: '22px',
                    letterSpacing: '0.08em',
                    cursor: valid ? 'pointer' : 'not-allowed',
                  }}
                >
                  {textos.qr_send ?? 'SEND'}
                </button>
              </div>
            </div>
          </div>

          <div className="absolute inset-x-0" style={{ bottom: 0 }}>
            <div className="flex w-full justify-center">
              <div
                style={{
                  display: 'inline-flex',
                  padding: '26px 26px 32px 26px',
                  backgroundColor: '#ffffff',
                  borderRadius: '12px 12px 0 0',
                  boxShadow: '0 -8px 20px rgba(0,0,0,0.15)',
                }}
              >
                <NumericKeypad onKey={handleKey} />
              </div>
            </div>
          </div>
        </div>
      );
    }
    ```

    **Nota:** las keys de `textos` son GENÉRICAS (`qr_instruction`, `qr_phone_label`, `qr_country`, `qr_phone_placeholder`, `qr_phone_aria`, `qr_terms`, `qr_send`). Cada consumidor (PassShareHost, TicketPurchaseHost) mapea sus propias keys (`passes_share_instruction` / `tickets_share_instruction` / etc.) al shape genérico antes de pasarlas como prop.
  </action>
  <verify>
    - `pnpm typecheck` pasa limpio.
    - `ls src/components/shared/qr-purchase-modal.tsx` existe.
    - `grep -n "priceDisplay\|purchaseUrl" src/components/shared/qr-purchase-modal.tsx` muestra props usadas.
  </verify>
  <done>
    `QrPurchaseModal` generic exportado con props `{open, title, purchaseUrl, priceDisplay?, textos, qrLogo?, onCancel, onSent}`.
  </done>
</task>

<task type="auto">
  <name>T3: Crear src/components/shared/qr-purchase-host.tsx con eventName configurable</name>
  <files>src/components/shared/qr-purchase-host.tsx, src/components/passes/pass-sent-confirmation.tsx (move)</files>
  <action>
    **Mover** (no copiar) `src/components/passes/pass-sent-confirmation.tsx` → `src/components/shared/sent-confirmation.tsx`. Usar `mv` del sistema o Write en la ubicación nueva + eliminación del original.

    El contenido es exactamente el mismo (sin cambios de lógica — solo path y nombre de export si aplica). El componente actual se llama `PassSentConfirmation` — renombrar el export a `SentConfirmation` y ajustar los imports downstream (se arreglan en T4).

    **Crear** `src/components/shared/qr-purchase-host.tsx`:

    ```tsx
    'use client';

    import { useEffect, useState } from 'react';

    import { SentConfirmation } from './sent-confirmation';
    import { QrPurchaseModal } from './qr-purchase-modal';

    interface Props {
      /** Nombre del CustomEvent a escuchar (ej. 'kiosk:pass-share-open' o 'kiosk:ticket-purchase-open'). */
      eventName: string;
      /** Título que pasará al modal. */
      title: string;
      /** URL del QR y payload del SMS. */
      purchaseUrl: string;
      /** Precio opcional (Tickets). */
      priceDisplay?: string;
      textos: Record<string, string>;
      qrLogo?: string;
      /** Textos del sent-confirmation: título y mensaje. */
      sentTitle: string;
      sentMessage: string;
      /** Callback llamado al completar el envío (para telemetría / CustomEvent downstream). */
      onSent?: (phoneDigits: string) => void;
    }

    type Phase = 'closed' | 'share' | 'sent';

    /**
     * Host compartido del modal de compra/share QR. Escucha un CustomEvent
     * parametrizable, abre el `QrPurchaseModal` y muestra `SentConfirmation`
     * al completar el envío.
     *
     * Montado como sibling del detail en la ruta `[slug]/page.tsx`.
     */
    export function QrPurchaseHost({
      eventName,
      title,
      purchaseUrl,
      priceDisplay,
      textos,
      qrLogo,
      sentTitle,
      sentMessage,
      onSent,
    }: Props) {
      const [phase, setPhase] = useState<Phase>('closed');

      useEffect(() => {
        const onOpen = () => setPhase('share');
        window.addEventListener(eventName, onOpen);
        return () => window.removeEventListener(eventName, onOpen);
      }, [eventName]);

      const handleSent = (phoneDigits: string) => {
        onSent?.(phoneDigits);
        setPhase('sent');
      };

      return (
        <>
          <QrPurchaseModal
            open={phase === 'share'}
            title={title}
            purchaseUrl={purchaseUrl}
            priceDisplay={priceDisplay}
            textos={textos}
            qrLogo={qrLogo}
            onCancel={() => setPhase('closed')}
            onSent={handleSent}
          />
          {phase === 'sent' ? (
            <SentConfirmation
              title={sentTitle}
              message={sentMessage}
              onAutoClose={() => setPhase('closed')}
            />
          ) : null}
        </>
      );
    }
    ```

    **No eliminar** aún `pass-share-modal.tsx` ni `pass-share-host.tsx` — se elimina en T4 después de actualizar imports.
  </action>
  <verify>
    - `pnpm typecheck` pasa limpio.
    - `ls src/components/shared/` muestra `qr-purchase-modal.tsx`, `qr-purchase-host.tsx`, `sent-confirmation.tsx`.
    - `grep -n "SentConfirmation" src/components/shared/sent-confirmation.tsx` muestra export.
  </verify>
  <done>
    Host y confirmation compartidos en `src/components/shared/`. `eventName` parametrizable.
  </done>
</task>

<task type="auto">
  <name>T4: Migrar Passes al shared host + eliminar archivos viejos</name>
  <files>src/components/passes/pass-detail-with-share.tsx, src/components/passes/pass-share-modal.tsx (delete), src/components/passes/pass-share-host.tsx (delete), src/app/(kiosk)/home/[module]/[slug]/page.tsx</files>
  <action>
    **Editar** `src/components/passes/pass-detail-with-share.tsx`. El contenido completo queda:

    ```tsx
    'use client';

    import type { PassItem } from '@/lib/config';

    import { PassDetail } from './pass-detail';

    interface Props {
      moduleKey: string;
      pass: PassItem;
      textos: Record<string, string>;
    }

    export function PassDetailWithShare({ moduleKey, pass, textos }: Props) {
      const handleShareOpen = () => {
        window.dispatchEvent(new CustomEvent('kiosk:pass-share-open'));
      };
      return (
        <PassDetail moduleKey={moduleKey} pass={pass} textos={textos} onShareOpen={handleShareOpen} />
      );
    }
    ```

    (Sin cambios vs. actual — solo confirmar.)

    **Editar** `src/app/(kiosk)/home/[module]/[slug]/page.tsx` — rama `if (mod.kind === 'passes')`. Reemplazar el bloque:

    ```tsx
    import { PassShareHost } from '@/components/passes/pass-share-host';
    // ...
    <PassShareHost
      client={{ slug: config.client.slug }}
      pass={pass}
      textos={config.textos ?? {}}
      qrLogo={mod.qrLogo}
    />
    ```

    Por:

    ```tsx
    import { QrPurchaseHost } from '@/components/shared/qr-purchase-host';
    import { buildShareResult, dispatchShareResult } from '@/lib/passes';
    // ...
    <QrPurchaseHost
      eventName="kiosk:pass-share-open"
      title={pass.title.toUpperCase()}
      purchaseUrl={pass.bandwangoUrl}
      qrLogo={mod.qrLogo}
      textos={{
        qr_instruction: config.textos?.passes_share_instruction ?? 'SCAN THIS QR CODE TO HAVE YOUR PASS',
        qr_phone_label: config.textos?.passes_share_phone_label ?? 'Enter your phone number',
        qr_country: config.textos?.passes_share_country ?? 'USA (+1)',
        qr_phone_placeholder: config.textos?.passes_share_phone_placeholder ?? '000-555-0115',
        qr_phone_aria: config.textos?.passes_share_phone_aria ?? 'Phone number. Tap to edit via keypad.',
        qr_terms: config.textos?.passes_share_terms ?? 'I accept details',
        qr_send: config.textos?.passes_share_send ?? 'SEND',
      }}
      sentTitle={config.textos?.passes_sent_title ?? 'You are all set!'}
      sentMessage={config.textos?.passes_sent_message ?? 'The pass was successfully sent'}
      onSent={(phoneDigits) => {
        const result = buildShareResult({
          client: config.client.slug,
          passSlug: pass.slug,
          passTitle: pass.title,
          phone: phoneDigits,
          bandwangoUrl: pass.bandwangoUrl,
        });
        dispatchShareResult(result);
      }}
    />
    ```

    **Eliminar** los archivos viejos:
    ```bash
    rm src/components/passes/pass-share-modal.tsx
    rm src/components/passes/pass-share-host.tsx
    rm src/components/passes/pass-sent-confirmation.tsx
    ```
    (El último se movió a `src/components/shared/sent-confirmation.tsx` en T3.)
  </action>
  <verify>
    - `pnpm check` pasa limpio (typecheck + lint + format).
    - `ls src/components/passes/` NO tiene `pass-share-*` ni `pass-sent-confirmation.tsx`.
    - `grep -rn "pass-share-host\|PassShareHost\|pass-share-modal" src/` retorna vacío.
    - Playwright manual: `/home/passes/museum-pass` → tap GET YOURS → aparece modal QR + phone → SEND → aparece "You are all set!" con check lime. Comportamiento IDÉNTICO al pre-refactor.
  </verify>
  <done>
    Passes usa `QrPurchaseHost` compartido. Archivos viejos eliminados. Regresión visual OK.
  </done>
</task>

<task type="auto">
  <name>T5: Añadir textos tickets_* a default + template + keys qr_* comunes</name>
  <files>clients/default/config.json, clients/_template/config.json</files>
  <action>
    En `clients/default/config.json`, localizar el bloque `"textos": {...}`. Al FINAL del bloque (antes del `}` de cierre y su coma), añadir:

    ```json
    ,
    "tickets_label": "Tickets",
    "tickets_buy_cta": "BUY TICKET",
    "tickets_share_instruction": "SCAN QR OR GET SMS TO BUY YOUR TICKET",
    "tickets_share_phone_label": "Enter your phone number",
    "tickets_share_phone_placeholder": "000-555-0115",
    "tickets_share_country": "USA (+1)",
    "tickets_share_terms": "I accept details",
    "tickets_share_send": "SEND",
    "tickets_sent_title": "Link sent!",
    "tickets_sent_message": "Check your phone to complete the purchase.",
    "tickets_empty": "No tickets available right now.",
    "tickets_share_phone_aria": "Phone number. Tap to edit via keypad."
    ```

    Repetir la misma adición en `clients/_template/config.json` (mismas 12 keys).

    **No** modificar keys `passes_*` existentes — se reusan vía el mapeo en la rama passes que hicimos en T4.
  </action>
  <verify>
    - `python3 -c "import json; json.load(open('clients/default/config.json'))"` sin errores.
    - `python3 -c "import json; json.load(open('clients/_template/config.json'))"` sin errores.
    - `grep -c "tickets_" clients/default/config.json` devuelve 12.
    - `grep -c "tickets_" clients/_template/config.json` devuelve 12.
  </verify>
  <done>
    12 keys `tickets_*` añadidas a default y template.
  </done>
</task>

<task type="auto">
  <name>T6: Añadir bloque modules.tickets y tile tickets a config default</name>
  <files>clients/default/config.json, clients/default/assets/home/tickets.jpg (copy)</files>
  <action>
    **Preparar asset** del tile. Dentro de `clients/default/assets/home/tiles/` (o donde vivan los tiles según T12.b del explore), copiar el asset del tile `events`:
    ```bash
    cp clients/default/assets/home/tiles/events.jpg clients/default/assets/home/tiles/tickets.jpg
    ```
    (Placeholder — cliente real lo reemplaza después.)

    **Editar** `clients/default/config.json` bloque `features.home.tiles[]`. Localizar el tile `events` y añadir INMEDIATAMENTE DESPUÉS:
    ```json
    ,
    {
      "key": "tickets",
      "label": "Tickets",
      "enabled": true,
      "image": "/assets/home/tiles/tickets.jpg"
    }
    ```

    **Editar** `features.home.modules` del mismo archivo. Localizar el cierre del bloque `events: {...}` y añadir DESPUÉS:
    ```json
    ,
    "tickets": {
      "kind": "tickets",
      "label": "Tickets",
      "heroImage": "https://images.unsplash.com/photo-1470229722913-7c0e2dbbafd3?w=1920&q=80"
    }
    ```

    (Las `categories`, `venues`, `features` se omiten — el filter overlay las derivará del pool visible en T11.)
  </action>
  <verify>
    - `python3 -c "import json; c=json.load(open('clients/default/config.json')); t=c['features']['home']['tiles']; m=c['features']['home']['modules']; print([x['key'] for x in t if x.get('key')=='tickets'], 'tickets' in m)"` imprime `['tickets'] True`.
    - `ls clients/default/assets/home/tiles/tickets.jpg` existe.
    - Playwright: abrir `http://localhost:3000/home/tickets` — no 404, renderiza (aunque vacío, lo resolvemos en Ola 2).
  </verify>
  <done>
    Tile "Tickets" añadido. Bloque `modules.tickets` con kind/label/heroImage. Asset placeholder copiado.
  </done>
</task>

<task type="auto">
  <name>T7: Insertar 10 events ticketables distribuidos en modules.events.events[]</name>
  <files>clients/default/config.json</files>
  <action>
    En `clients/default/config.json` → `features.home.modules.events.events[]` (46 events actuales, líneas aprox 4794-6371).

    Insertar estos 10 events en posiciones distribuidas (no al final). Sugerencia: cada ~4-5 events existentes, insertar uno ticketable. Mantener orden cronológico si se puede, pero es aceptable mezclar. Mix de precios:

    - 3× `"$XX"`: `"$25"`, `"$18"`, `"$40"`
    - 3× `"$XX–YY"`: `"$15–30"`, `"$45–80"`, `"$20–35"`
    - 2× `"From $XX"`: `"From $10"`, `"From $22"`
    - 2× premium `"$XXX"`: `"$120"`, `"$150"`

    Cada ticketable event tiene la forma:
    ```json
    {
      "slug": "<unique-slug>",
      "title": "<Title>",
      "category": "Music|Sports|Festival|Family|Arts",
      "image": "<unsplash URL verified>",
      "date": "2026-04-XX",
      "startTime": "HH:MM",
      "endTime": "HH:MM",
      "venue": "<Venue name>",
      "priceMode": "paid",
      "priceBand": 1|2|3|4,
      "features": ["Family-friendly"|"Outdoor"|"Indoor"|"Parking"|...],
      "popularity": 50..90,
      "address": "<address>",
      "phone": "(602) 555-XXXX",
      "coords": { "lat": 33.4X, "lng": -112.0X },
      "website": "https://example.com/...",
      "description": "<desc>",
      "directions": [],
      "ticket": {
        "priceDisplay": "<según mix>",
        "purchaseUrl": "https://tickets.example.com/<slug>"
      }
    }
    ```

    **Lista de 10 events sugerida** (variados en category + date):

    1. `symphony-spring-night` — Music · $40 · 2026-04-10 19:30 · Symphony Hall · priceBand 3 · "Indoor", "Reserved seating"
    2. `indie-festival-2026` — Festival · $15–30 · 2026-04-11 16:00 · Roosevelt Row · priceBand 2 · "Outdoor", "Food trucks", "Family-friendly"
    3. `cardinals-preseason` — Sports · $25 · 2026-04-13 13:00 · State Farm Stadium · priceBand 2 · "Outdoor", "Parking"
    4. `opera-la-traviata` — Arts · $45–80 · 2026-04-15 19:00 · Phoenix Opera House · priceBand 4 · "Indoor", "Reserved seating"
    5. `stand-up-comedy-night` — Arts · $18 · 2026-04-16 20:00 · Comedy Club · priceBand 1 · "Indoor"
    6. `food-wine-expo` — Festival · $120 · 2026-04-18 11:00 · Convention Center · priceBand 4 · "Indoor", "21+"
    7. `family-movie-matinee` — Family · $10 · 2026-04-20 14:00 · Harkins Theatre · priceBand 1 · "Indoor", "Family-friendly"
    8. `jazz-in-the-park` — Music · $20–35 · 2026-04-22 18:00 · Desert Botanical Garden · priceBand 2 · "Outdoor", "Picnic"
    9. `marathon-relay-race` — Sports · $22 · 2026-04-24 06:30 · Papago Park · priceBand 2 · "Outdoor", "Family-friendly"
    10. `gala-fundraiser` — Arts · $150 · 2026-04-25 18:30 · Biltmore Hotel · priceBand 4 · "Indoor", "21+", "Formal"

    **Verificar URLs de imágenes ANTES de escribir al JSON.** Desde un browser con Playwright MCP abierto, ejecutar `fetch` HEAD sobre cada URL de Unsplash candidata. Si alguna retorna 404, sustituir por otra. URLs sugeridas (verificar primero):

    1. `https://images.unsplash.com/photo-1465847899084-d164df4dedc6?w=600&q=80` (symphony)
    2. `https://images.unsplash.com/photo-1459749411175-04bf5292ceea?w=600&q=80` (indie fest)
    3. `https://images.unsplash.com/photo-1461988091159-192b6df7054f?w=600&q=80` (stadium football)
    4. `https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=600&q=80` (opera)
    5. `https://images.unsplash.com/photo-1527224538127-2104bb71c51b?w=600&q=80` (comedy)
    6. `https://images.unsplash.com/photo-1414235077428-338989a2e8c0?w=600&q=80` (food)
    7. `https://images.unsplash.com/photo-1489599849927-2ee91cede3ba?w=600&q=80` (movie)
    8. `https://images.unsplash.com/photo-1415201364774-f6f0bb35f28f?w=600&q=80` (jazz)
    9. `https://images.unsplash.com/photo-1486218119243-13883505764c?w=600&q=80` (marathon)
    10. `https://images.unsplash.com/photo-1511795409834-ef04bbd61622?w=600&q=80` (gala)

    **Distribuir** estos 10 entre los 46 existentes: `[5 existentes, 1 nuevo, 5 existentes, 1 nuevo, ...]` aprox.

    `pnpm format` al finalizar para prettier-align.
  </action>
  <verify>
    - `python3 -c "import json; c=json.load(open('clients/default/config.json')); e=c['features']['home']['modules']['events']['events']; t=[x for x in e if 'ticket' in x]; print('total:', len(e), 'ticketable:', len(t), 'prices:', [x['ticket']['priceDisplay'] for x in t])"` imprime `total: 56 ticketable: 10 prices: [...]`.
    - Playwright evaluate fetch HEAD sobre las 10 URLs de imágenes. Cero rotas.
  </verify>
  <done>
    10 events nuevos con `.ticket` en seed. Total 56 events. 10 URLs verificadas HTTP 200.
  </done>
</task>

<task type="auto">
  <name>COMMIT Ola 1: foundations + shared QR purchase + seed tickets</name>
  <files>n/a (git)</files>
  <action>
    ```bash
    git add src/lib/config.ts \
            src/components/shared/qr-purchase-modal.tsx \
            src/components/shared/qr-purchase-host.tsx \
            src/components/shared/sent-confirmation.tsx \
            src/components/passes/pass-detail-with-share.tsx \
            src/app/\(kiosk\)/home/\[module\]/\[slug\]/page.tsx \
            clients/default/config.json \
            clients/default/assets/home/tiles/tickets.jpg \
            clients/_template/config.json
    git rm src/components/passes/pass-share-modal.tsx \
           src/components/passes/pass-share-host.tsx \
           src/components/passes/pass-sent-confirmation.tsx
    git commit -m "$(cat <<'EOF'
    feat(tickets): foundations + shared QR purchase modal

    Ola 1/4 Fase 3.11 — spec docs/superpowers/specs/2026-04-22-tickets-module-design.md.

    - EventItem + ticket? opcional (priceDisplay + purchaseUrl)
    - HomeTicketsModule (kind: 'tickets') + variant union
    - Extract Passes share flow → src/components/shared/{qr-purchase-modal,qr-purchase-host,sent-confirmation}.tsx
    - Passes migrado al host compartido con mapeo textos passes_* → qr_*
    - Seed: 10 events ticketables mezclados (total 56) + tile + modules.tickets
    - 12 textos tickets_* en default + template
    EOF
    )"
    ```
  </action>
  <verify>
    - `git log -1 --oneline` muestra el commit.
    - `git status` limpio salvo archivos de Olas 2-4 pendientes.
  </verify>
  <done>
    Commit de Ola 1 hecho. Passes sigue funcionando (regresión validada en T4).
  </done>
</task>

---

## Ola 2 — Tickets listing (card + grid + toolbar + module + ruta)

<task type="auto">
  <name>T8: Crear src/lib/tickets.ts con filterTicketableEvents</name>
  <files>src/lib/tickets.ts</files>
  <action>
    Crear `src/lib/tickets.ts`:

    ```ts
    import type { EventItem } from './config';

    /**
     * Filtra la lista de events dejando solo los que tienen el campo `ticket`
     * presente (eventos con venta de boletos activa).
     *
     * Usado por `TicketsModule` — Tickets ⊂ Events.
     */
    export function filterTicketableEvents(events: readonly EventItem[]): EventItem[] {
      return events.filter((e): e is EventItem & { ticket: NonNullable<EventItem['ticket']> } => e.ticket != null);
    }

    /** Deriva catálogo de categorías visibles del pool de tickets. */
    export function deriveTicketCategories(events: readonly EventItem[]): string[] {
      const set = new Set<string>();
      for (const e of events) set.add(e.category);
      return [...set].sort();
    }

    /** Deriva catálogo de venues visibles del pool de tickets. */
    export function deriveTicketVenues(events: readonly EventItem[]): string[] {
      const set = new Set<string>();
      for (const e of events) set.add(e.venue);
      return [...set].sort();
    }

    /** Deriva catálogo de features (unión) del pool de tickets. */
    export function deriveTicketFeatures(events: readonly EventItem[]): string[] {
      const set = new Set<string>();
      for (const e of events) for (const f of e.features) set.add(f);
      return [...set].sort();
    }
    ```
  </action>
  <verify>
    - `pnpm typecheck` pasa.
    - `grep -n "filterTicketableEvents\|deriveTicket" src/lib/tickets.ts` muestra 4 exports.
  </verify>
  <done>
    Helpers de filtrado y derivación de facets exportados.
  </done>
</task>

<task type="auto">
  <name>T9: Crear src/components/tickets/ticket-card.tsx con badge de precio</name>
  <files>src/components/tickets/ticket-card.tsx</files>
  <action>
    Crear `src/components/tickets/ticket-card.tsx` basado en `event-card.tsx` pero con badge pill superpuesto al cover. Contenido:

    ```tsx
    'use client';

    import Link from 'next/link';
    import { useState } from 'react';

    import type { EventItem } from '@/lib/config';
    import { formatTime12 } from '@/lib/events-date';

    interface Props {
      event: EventItem & { ticket: NonNullable<EventItem['ticket']> };
      moduleKey: string;
      showGutter?: boolean;
    }

    export function TicketCard({ event, moduleKey, showGutter = true }: Props) {
      const [imgError, setImgError] = useState(false);
      const date = new Date(event.date + 'T00:00:00');
      const weekdayLong = date.toLocaleDateString('en-US', { weekday: 'long' }).toUpperCase();
      const dayNumber = date.getDate();

      return (
        <Link
          href={`/home/${moduleKey}/${event.slug}`}
          className="block focus:outline-none focus-visible:ring-4 focus-visible:ring-primary/40"
          aria-label={`${event.title} — ${event.ticket.priceDisplay}`}
        >
          <div
            className="flex"
            style={{ width: '880px', columnGap: '20px', marginBottom: '22px' }}
          >
            {showGutter ? (
              <div className="flex flex-col items-center justify-center font-sans" style={{ width: '84px', flexShrink: 0 }}>
                <span style={{ fontSize: '14px', fontWeight: 700, color: '#3b3b3b', letterSpacing: '0.08em' }}>
                  {weekdayLong}
                </span>
                <span style={{ fontSize: '60px', fontWeight: 700, color: '#3b3b3b', lineHeight: '1' }}>
                  {dayNumber}
                </span>
              </div>
            ) : null}

            <div
              className="relative flex overflow-hidden"
              style={{
                flex: 1,
                height: '158px',
                borderRadius: '8px',
                backgroundColor: '#3b3b3b',
                boxShadow: '0 6px 14px rgba(0,0,0,0.12)',
              }}
            >
              <div style={{ width: '158px', height: '158px', flexShrink: 0, position: 'relative' }}>
                {imgError ? (
                  <div
                    className="absolute inset-0"
                    style={{
                      background: 'linear-gradient(135deg, hsl(var(--primary)) 0%, hsl(var(--primary) / 0.7) 100%)',
                    }}
                  />
                ) : (
                  // eslint-disable-next-line @next/next/no-img-element
                  <img
                    src={event.image}
                    alt=""
                    onError={() => setImgError(true)}
                    className="absolute inset-0 h-full w-full object-cover"
                  />
                )}
                {/* Badge de precio — pill blanco con texto primary */}
                <div
                  className="absolute font-display font-bold"
                  style={{
                    top: '12px',
                    right: '12px',
                    paddingLeft: '14px',
                    paddingRight: '14px',
                    paddingTop: '6px',
                    paddingBottom: '6px',
                    borderRadius: '999px',
                    backgroundColor: '#ffffff',
                    color: '#004f8b',
                    fontSize: '15px',
                    letterSpacing: '0.02em',
                    boxShadow: '0 2px 6px rgba(0,0,0,0.25)',
                  }}
                >
                  {event.ticket.priceDisplay}
                </div>
              </div>

              <div className="flex flex-1 flex-col justify-center" style={{ padding: '0 28px', color: '#ffffff' }}>
                <span
                  className="font-sans"
                  style={{ fontSize: '22px', fontWeight: 700, lineHeight: '1.2', marginBottom: '6px' }}
                >
                  {event.title}
                </span>
                <span
                  className="font-sans"
                  style={{ fontSize: '16px', color: 'rgba(255,255,255,0.82)' }}
                >
                  {formatTime12(event.startTime)} · {event.venue}
                </span>
              </div>
            </div>
          </div>
        </Link>
      );
    }
    ```

    Asumir que `formatTime12` ya existe en `src/lib/events-date.ts` (de Fase 3.4). Si no, revisar y ajustar import.
  </action>
  <verify>
    - `pnpm typecheck` pasa.
    - `grep -n "priceDisplay" src/components/tickets/ticket-card.tsx` muestra uso del campo.
    - `pnpm lint` pasa (solo warnings pre-existentes aceptados).
  </verify>
  <done>
    `TicketCard` con badge pill de precio top-right sobre el cover + fallback de imagen.
  </done>
</task>

<task type="auto">
  <name>T10: Crear src/components/tickets/tickets-list.tsx</name>
  <files>src/components/tickets/tickets-list.tsx</files>
  <action>
    Crear `src/components/tickets/tickets-list.tsx`:

    ```tsx
    import type { EventItem } from '@/lib/config';

    import { TicketCard } from './ticket-card';

    interface Props {
      tickets: readonly (EventItem & { ticket: NonNullable<EventItem['ticket']> })[];
      moduleKey: string;
      emptyLabel?: string;
    }

    export function TicketsList({ tickets, moduleKey, emptyLabel }: Props) {
      if (tickets.length === 0) {
        return (
          <div
            className="flex h-full w-full items-start justify-center font-sans text-gray-500"
            style={{ paddingTop: '80px', fontSize: '22px' }}
          >
            {emptyLabel ?? 'No tickets available right now.'}
          </div>
        );
      }
      return (
        <div
          style={{
            paddingTop: '32px',
            paddingBottom: '40px',
            paddingLeft: '140px',
            paddingRight: '60px',
          }}
        >
          {tickets.map((t) => (
            <TicketCard key={t.slug} event={t} moduleKey={moduleKey} />
          ))}
        </div>
      );
    }
    ```
  </action>
  <verify>
    - `pnpm typecheck` pasa.
  </verify>
  <done>
    `TicketsList` con empty state y loop.
  </done>
</task>

<task type="auto">
  <name>T11: Crear src/components/tickets/tickets-filter-overlay.tsx (copia adaptada de Events)</name>
  <files>src/components/tickets/tickets-filter-overlay.tsx</files>
  <action>
    **Copiar** `src/components/events/events-filter-overlay.tsx` a `src/components/tickets/tickets-filter-overlay.tsx`. Cambiar:

    1. Nombre del componente `EventsFilterOverlay` → `TicketsFilterOverlay`.
    2. Prop `mod: HomeEventsModule` → `mod: HomeTicketsModule & { derivedCategories: string[]; derivedVenues: string[]; derivedFeatures: string[] }` — o simplemente:
       - Prop `mod: HomeTicketsModule`
       - Añadir prop `catalogue: { categories: string[]; venues: string[]; features: string[] }` que pasa `TicketsModule` derivado del pool visible.
    3. Import type `HomeEventsModule` → `HomeTicketsModule`.
    4. Usar `catalogue.categories` / `catalogue.venues` / `catalogue.features` en vez de `mod.categories` / etc.

    **El resto del componente (4 secciones Features AND + Category OR + Venue OR + Price OR, overlay positioning, CLEAR/APPLY buttons) NO se toca.**

    Si el componente de Events usa `EventsFilterState` directamente desde `src/lib/events-filter.ts`, Tickets **reusa el mismo tipo** — no crear `TicketsFilterState` nuevo porque es idéntico.
  </action>
  <verify>
    - `pnpm typecheck` pasa.
    - `diff src/components/events/events-filter-overlay.tsx src/components/tickets/tickets-filter-overlay.tsx` muestra solo los cambios de nombres/props (no hay cambios estructurales).
  </verify>
  <done>
    `TicketsFilterOverlay` con 4 dimensiones + catálogos derivados dinámicamente.
  </done>
</task>

<task type="auto">
  <name>T12: Crear src/components/tickets/tickets-module.tsx</name>
  <files>src/components/tickets/tickets-module.tsx</files>
  <action>
    Crear `src/components/tickets/tickets-module.tsx`:

    ```tsx
    'use client';

    import type { ReactNode } from 'react';
    import { useMemo, useState } from 'react';

    import { SearchOverlay } from '@/components/home/search-overlay';
    import { ListingsToolbar } from '@/components/listings/listings-toolbar';
    import { FloatingHomeButton } from '@/components/listings/floating-home-button';
    import { SortOverlay } from '@/components/listings/sort-overlay';
    import { WeekPicker } from '@/components/events/week-picker';
    import type { HomeListing, HomeTicketsModule, EventItem } from '@/lib/config';
    import { EMPTY_EVENTS_FILTER, applyEventsFilters, type EventsFilterState } from '@/lib/events-filter';
    import { sortEvents, type EventsSortOrder } from '@/lib/events-sort';
    import { todayISO } from '@/lib/events-date';
    import {
      filterTicketableEvents,
      deriveTicketCategories,
      deriveTicketVenues,
      deriveTicketFeatures,
    } from '@/lib/tickets';

    import { TicketsList } from './tickets-list';
    import { TicketsFilterOverlay } from './tickets-filter-overlay';

    interface Props {
      moduleKey: string;
      module: HomeTicketsModule;
      /** Pool completo de events del cliente — se filtra aquí por ticket presente. */
      allEvents: readonly EventItem[];
      clientCoords?: { lat: number; lng: number };
      clientTimezone?: string;
      textos: Record<string, string>;
      header: ReactNode;
    }

    export function TicketsModule({
      moduleKey,
      module: mod,
      allEvents,
      clientCoords,
      clientTimezone,
      textos,
      header,
    }: Props) {
      const today = useMemo(() => todayISO(clientTimezone), [clientTimezone]);
      const [selectedDate, setSelectedDate] = useState<string>(today);
      const [filter, setFilter] = useState<EventsFilterState>(EMPTY_EVENTS_FILTER);
      const [sort, setSort] = useState<EventsSortOrder>('date');
      const [searchOpen, setSearchOpen] = useState(false);
      const [filterOpen, setFilterOpen] = useState(false);
      const [sortOpen, setSortOpen] = useState(false);

      const allTickets = useMemo(() => filterTicketableEvents(allEvents), [allEvents]);

      const catalogue = useMemo(
        () => ({
          categories: mod.categories ?? deriveTicketCategories(allTickets),
          venues: mod.venues ?? deriveTicketVenues(allTickets),
          features: mod.features ?? deriveTicketFeatures(allTickets),
        }),
        [mod.categories, mod.venues, mod.features, allTickets],
      );

      const visibleTickets = useMemo(() => {
        const byDay = allTickets.filter((e) => e.date === selectedDate);
        return sortEvents(applyEventsFilters(byDay, filter), sort, clientCoords);
      }, [allTickets, selectedDate, filter, sort, clientCoords]) as (EventItem & { ticket: NonNullable<EventItem['ticket']> })[];

      const searchPool: HomeListing[] = useMemo(
        () =>
          allTickets.map((t) => ({
            slug: t.slug,
            title: t.title,
            category: moduleKey,
            image: t.image,
          })),
        [allTickets, moduleKey],
      );

      return (
        <div className="relative flex h-full w-full flex-col overflow-hidden bg-white">
          {header}
          <ListingsToolbar
            label={mod.label}
            onSearch={() => setSearchOpen(true)}
            onSort={() => setSortOpen(true)}
            onFilter={() => setFilterOpen(true)}
            activeSort={sort !== 'date'}
          />
          <WeekPicker selectedDate={selectedDate} onSelectDate={setSelectedDate} />
          <main className="scrollbar-hide relative flex-1 overflow-y-auto overflow-x-hidden overscroll-contain">
            <TicketsList
              tickets={visibleTickets}
              moduleKey={moduleKey}
              emptyLabel={textos.tickets_empty}
            />
          </main>
          <FloatingHomeButton />
          {searchOpen ? (
            <SearchOverlay listings={searchPool} onClose={() => setSearchOpen(false)} />
          ) : null}
          {filterOpen ? (
            <TicketsFilterOverlay
              open={filterOpen}
              mod={mod}
              catalogue={catalogue}
              initial={filter}
              onCancel={() => setFilterOpen(false)}
              onApply={(next) => {
                setFilter(next);
                setFilterOpen(false);
              }}
            />
          ) : null}
          {sortOpen ? (
            <SortOverlay
              open={sortOpen}
              current={sort}
              onCancel={() => setSortOpen(false)}
              onApply={(next) => {
                setSort(next as EventsSortOrder);
                setSortOpen(false);
              }}
            />
          ) : null}
        </div>
      );
    }
    ```

    Si los imports `sortEvents`, `EventsSortOrder`, `todayISO`, `SortOverlay` no están disponibles, ajustar a los que sí existen (basado en lo que usa `EventsModule`).
  </action>
  <verify>
    - `pnpm typecheck` pasa.
    - `pnpm lint` sin errores nuevos.
  </verify>
  <done>
    `TicketsModule` con filter/sort/search + WeekPicker + TicketsList + facets derivados.
  </done>
</task>

<task type="auto">
  <name>T13: Añadir rama 'tickets' a src/app/(kiosk)/home/[module]/page.tsx</name>
  <files>src/app/(kiosk)/home/[module]/page.tsx</files>
  <action>
    Localizar el bloque `if (mod?.kind === 'passes') { ... }` en `src/app/(kiosk)/home/[module]/page.tsx`. Añadir INMEDIATAMENTE DESPUÉS:

    ```tsx
    if (mod?.kind === 'tickets') {
      const eventsModule = config.features?.home?.modules?.events;
      const allEvents = eventsModule?.kind === 'events' ? eventsModule.events : [];
      return (
        <KioskCanvas>
          <TicketsModule
            moduleKey={module}
            module={mod}
            allEvents={allEvents}
            clientCoords={config.client.coords}
            clientTimezone={config.client.timezone}
            textos={config.textos ?? {}}
            header={<HomeHeader heroImage={mod.heroImage} showLanguage={false} />}
          />
          <AdsSlot ads={ads} />
        </KioskCanvas>
      );
    }
    ```

    Añadir el import arriba:
    ```tsx
    import { TicketsModule } from '@/components/tickets/tickets-module';
    ```
  </action>
  <verify>
    - `pnpm check` pasa limpio.
    - Playwright: abrir `http://localhost:3000/home/tickets` → renderiza listing con 10 ticket-cards (1-2 visibles según la fecha actual — WeekPicker filtra por día). Cambiar fecha del WeekPicker a una con tickets muestra las cards.
    - Playwright: badge de precio visible pill blanco con texto azul sobre el cover.
  </verify>
  <done>
    `/home/tickets` renderiza. Listing + filter + sort + search + WeekPicker operan.
  </done>
</task>

<task type="auto">
  <name>COMMIT Ola 2: tickets listing</name>
  <files>n/a (git)</files>
  <action>
    ```bash
    git add src/lib/tickets.ts \
            src/components/tickets/ \
            src/app/\(kiosk\)/home/\[module\]/page.tsx
    git commit -m "$(cat <<'EOF'
    feat(tickets): listing (card con badge precio + grid + filter + module + ruta)

    Ola 2/4 Fase 3.11.

    - src/lib/tickets.ts helpers (filter + derive facets)
    - TicketCard con pill badge top-right del cover (priceDisplay)
    - TicketsList vertical + empty state
    - TicketsFilterOverlay copia de Events adaptada a catalogue dinámico
    - TicketsModule compose toolbar + week-picker + list + overlays (reusa ListingsToolbar, WeekPicker, SearchOverlay, SortOverlay)
    - Rama tickets en /home/[module]
    EOF
    )"
    ```
  </action>
  <verify>
    - `git log -1 --oneline` muestra el commit.
  </verify>
  <done>
    Commit de Ola 2 hecho.
  </done>
</task>

---

## Ola 3 — Detail + buy flow

<task type="auto">
  <name>T14: Ampliar SecondaryCta en ListingDetail para soportar onClick</name>
  <files>src/components/listings/listing-detail.tsx</files>
  <action>
    En `src/components/listings/listing-detail.tsx`, localizar la interface `SecondaryCta` (alrededor de línea 37-42):

    ```tsx
    export interface SecondaryCta {
      label: string;
      href: string;
      color?: 'blue' | 'olive' | 'outline-red';
    }
    ```

    Reemplazar por:

    ```tsx
    export interface SecondaryCta {
      label: string;
      /** URL externa (apertura en tab nueva). Mutuamente exclusivo con onClick. */
      href?: string;
      /** Callback sincrónico. Mutuamente exclusivo con href. */
      onClick?: () => void;
      color?: 'blue' | 'olive' | 'outline-red';
    }
    ```

    Localizar `function SecondaryCtaButton({ cta }: { cta: SecondaryCta })` (alrededor de línea 439-467). Reemplazar el render `<a href={cta.href} ...>` por un render condicional:

    ```tsx
    function SecondaryCtaButton({ cta }: { cta: SecondaryCta }) {
      const color = cta.color ?? 'blue';
      const bg = color === 'olive' ? '#b9bd39' : '#1796d6';
      const isOutlineRed = color === 'outline-red';
      const sharedStyle = {
        position: 'absolute' as const,
        left: '609px',
        top: '665px',
        width: '260px',
        height: '64px',
        borderRadius: '8px',
        backgroundColor: isOutlineRed ? '#fff' : bg,
        border: isOutlineRed ? '3px solid #db323a' : 'none',
        color: isOutlineRed ? '#db323a' : '#fff',
        fontSize: '20px',
        fontWeight: 600,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      };

      if (cta.onClick) {
        return (
          <button type="button" onClick={cta.onClick} style={sharedStyle}>
            {cta.label}
          </button>
        );
      }
      return (
        <a href={cta.href} target="_blank" rel="noopener noreferrer" style={sharedStyle}>
          {cta.label}
        </a>
      );
    }
    ```
  </action>
  <verify>
    - `pnpm typecheck` pasa (si hay callers existentes de SecondaryCta, validar que siguen funcionando con `href` — ahora opcional).
    - `/home/restaurants/<slug>` con `reserveUrl` sigue renderizando el RESERVE NOW.
    - `/home/events/<slug>` con `ticketsUrl` legacy sigue renderizando GET TICKETS.
  </verify>
  <done>
    `SecondaryCta` acepta `href` o `onClick`. Lógica de render condicional.
  </done>
</task>

<task type="auto">
  <name>T15: Crear src/components/tickets/ticket-detail-with-buy.tsx</name>
  <files>src/components/tickets/ticket-detail-with-buy.tsx</files>
  <action>
    Crear `src/components/tickets/ticket-detail-with-buy.tsx`:

    ```tsx
    'use client';

    import { ListingDetail, type SecondaryCta } from '@/components/listings/listing-detail';
    import type { EventItem } from '@/lib/config';
    import { formatEventDateLong, formatTime12 } from '@/lib/events-date';

    interface Props {
      moduleKey: string;
      event: EventItem & { ticket: NonNullable<EventItem['ticket']> };
      textos: Record<string, string>;
    }

    /**
     * Wrapper client del `ListingDetail` para un ticket (event con `.ticket`).
     * Mapea el event a la shape de Listing y añade secondaryCta "BUY TICKET"
     * que dispara el CustomEvent `kiosk:ticket-purchase-open`.
     */
    export function TicketDetailWithBuy({ moduleKey, event, textos }: Props) {
      const eventMeta = [
        `${formatEventDateLong(event.date)} · ${formatTime12(event.startTime)}`,
        event.phone,
      ];

      const secondaryCta: SecondaryCta = {
        label: textos.tickets_buy_cta ?? 'BUY TICKET',
        onClick: () => window.dispatchEvent(new CustomEvent('kiosk:ticket-purchase-open')),
        color: 'blue',
      };

      // Mapear EventItem → HomeListing para reusar ListingDetail.
      const listingLike = {
        slug: event.slug,
        title: event.title,
        category: event.category,
        image: event.image,
        address: event.address,
        phone: event.phone,
        coords: event.coords,
        website: event.website,
        description: event.description,
        directions: event.directions,
        hours: undefined,
        features: event.features,
        popularity: event.popularity,
        threshold360Url: undefined,
      };

      return (
        <ListingDetail
          moduleKey={moduleKey}
          listing={listingLike as never}
          textos={textos}
          eventMeta={eventMeta}
          secondaryCta={secondaryCta}
          favoritesKind="event"
        />
      );
    }
    ```

    Si `ListingDetail` requiere props adicionales obligatorias, ajustar el shape de `listingLike` mirando el signature exacto.
  </action>
  <verify>
    - `pnpm typecheck` pasa.
    - `grep -n "tickets_buy_cta\|kiosk:ticket-purchase-open" src/components/tickets/ticket-detail-with-buy.tsx` muestra ambos.
  </verify>
  <done>
    Wrapper que renderiza ListingDetail con secondaryCta onClick disparando CustomEvent.
  </done>
</task>

<task type="auto">
  <name>T16: Añadir rama 'tickets' a [module]/[slug]/page.tsx con QrPurchaseHost</name>
  <files>src/app/(kiosk)/home/[module]/[slug]/page.tsx</files>
  <action>
    En `src/app/(kiosk)/home/[module]/[slug]/page.tsx`, añadir INMEDIATAMENTE DESPUÉS del bloque `if (mod.kind === 'events')` y ANTES del bloque `if (mod.kind === 'passes')`:

    ```tsx
    if (mod.kind === 'tickets') {
      const eventsModule = config.features?.home?.modules?.events;
      const allEvents = eventsModule?.kind === 'events' ? eventsModule.events : [];
      const event = allEvents.find((e) => e.slug === slug && e.ticket != null);
      if (!event || !event.ticket) notFound();
      return (
        <KioskCanvas>
          <TicketsModule
            moduleKey={module}
            module={mod}
            allEvents={allEvents}
            clientCoords={config.client.coords}
            clientTimezone={config.client.timezone}
            textos={config.textos ?? {}}
            header={<HomeHeader heroImage={mod.heroImage} showLanguage={false} />}
          />
          <TicketDetailWithBuy
            moduleKey={module}
            event={event as EventItem & { ticket: NonNullable<EventItem['ticket']> }}
            textos={config.textos ?? {}}
          />
          <QrPurchaseHost
            eventName="kiosk:ticket-purchase-open"
            title={event.title.toUpperCase()}
            purchaseUrl={event.ticket.purchaseUrl}
            priceDisplay={event.ticket.priceDisplay}
            textos={{
              qr_instruction: config.textos?.tickets_share_instruction ?? 'SCAN QR OR GET SMS TO BUY YOUR TICKET',
              qr_phone_label: config.textos?.tickets_share_phone_label ?? 'Enter your phone number',
              qr_country: config.textos?.tickets_share_country ?? 'USA (+1)',
              qr_phone_placeholder: config.textos?.tickets_share_phone_placeholder ?? '000-555-0115',
              qr_phone_aria: config.textos?.tickets_share_phone_aria ?? 'Phone number. Tap to edit via keypad.',
              qr_terms: config.textos?.tickets_share_terms ?? 'I accept details',
              qr_send: config.textos?.tickets_share_send ?? 'SEND',
            }}
            sentTitle={config.textos?.tickets_sent_title ?? 'Link sent!'}
            sentMessage={config.textos?.tickets_sent_message ?? 'Check your phone to complete the purchase.'}
          />
          <AdsSlot ads={ads} />
        </KioskCanvas>
      );
    }
    ```

    Añadir imports arriba:
    ```tsx
    import { TicketsModule } from '@/components/tickets/tickets-module';
    import { TicketDetailWithBuy } from '@/components/tickets/ticket-detail-with-buy';
    import { QrPurchaseHost } from '@/components/shared/qr-purchase-host';
    import type { EventItem } from '@/lib/config';
    ```
  </action>
  <verify>
    - `pnpm check` pasa limpio.
    - Playwright: `/home/tickets/symphony-spring-night` (o el slug que exista) → detail se muestra sobre listing → tap "BUY TICKET" → QR modal aparece con title "SYMPHONY SPRING NIGHT" + priceDisplay "$40" + QR + SEND. Completar flow con 10 dígitos + SEND → aparece "Link sent!" con check lime.
  </verify>
  <done>
    Ruta `/home/tickets/{slug}` funciona end-to-end.
  </done>
</task>

<task type="auto">
  <name>T17: Hacer que Events con .ticket también dispare QR purchase</name>
  <files>src/app/(kiosk)/home/[module]/[slug]/page.tsx</files>
  <action>
    En el mismo archivo, localizar la rama `if (mod.kind === 'events')` (alrededor de líneas 38-74 según exploración). Dentro del bloque, DESPUÉS de resolver `const event = ...` y ANTES del `return <KioskCanvas>`, añadir:

    ```tsx
    const hasTicket = event.ticket != null;
    ```

    En el `secondaryCta` que hoy usa `href={event.ticketsUrl}`, cambiar a condicional:

    ```tsx
    const secondaryCta: SecondaryCta | undefined = hasTicket
      ? {
          label: config.textos?.tickets_buy_cta ?? 'BUY TICKET',
          onClick: () => window.dispatchEvent(new CustomEvent('kiosk:ticket-purchase-open')),
          color: 'blue',
        }
      : event.ticketsUrl
        ? { label: 'GET TICKETS', href: event.ticketsUrl, color: 'blue' }
        : undefined;
    ```

    (El código exacto de la rama Events depende de su estructura actual — leer primero y adaptar. Mantener equivalencia funcional para events sin ticket.)

    Montar `<QrPurchaseHost>` como sibling del detail SOLO si `hasTicket`:

    ```tsx
    {hasTicket ? (
      <QrPurchaseHost
        eventName="kiosk:ticket-purchase-open"
        title={event.title.toUpperCase()}
        purchaseUrl={event.ticket!.purchaseUrl}
        priceDisplay={event.ticket!.priceDisplay}
        textos={{
          qr_instruction: config.textos?.tickets_share_instruction ?? 'SCAN QR OR GET SMS TO BUY YOUR TICKET',
          qr_phone_label: config.textos?.tickets_share_phone_label ?? 'Enter your phone number',
          qr_country: config.textos?.tickets_share_country ?? 'USA (+1)',
          qr_phone_placeholder: config.textos?.tickets_share_phone_placeholder ?? '000-555-0115',
          qr_phone_aria: config.textos?.tickets_share_phone_aria ?? 'Phone number. Tap to edit via keypad.',
          qr_terms: config.textos?.tickets_share_terms ?? 'I accept details',
          qr_send: config.textos?.tickets_share_send ?? 'SEND',
        }}
        sentTitle={config.textos?.tickets_sent_title ?? 'Link sent!'}
        sentMessage={config.textos?.tickets_sent_message ?? 'Check your phone to complete the purchase.'}
      />
    ) : null}
    ```
  </action>
  <verify>
    - `pnpm check` pasa limpio.
    - Playwright: `/home/events/symphony-spring-night` (event ticketable) → muestra CTA "BUY TICKET" → dispara QR modal con title y price correctos.
    - Playwright: `/home/events/<event-sin-ticket>` → sigue mostrando "GET TICKETS" con URL externa si existe `ticketsUrl`, o sin CTA secundario si no hay nada. **Sin regresión.**
  </verify>
  <done>
    Events con ticket dispara el mismo QR flow. Events sin ticket mantiene comportamiento legacy. Sin regresión.
  </done>
</task>

<task type="auto">
  <name>T18: Verificar integridad URLs imágenes de los 10 tickets</name>
  <files>n/a (QA)</files>
  <action>
    Con Playwright MCP abierto, ejecutar `evaluate` con `Promise.all(fetch HEAD)` sobre las 10 URLs de `image` de los events ticketables + la URL de `heroImage` del bloque `modules.tickets`. Reportar las que no respondan 200. Si hay rotas, buscar reemplazos (mismo patrón que hicimos con Passes) y actualizar `clients/default/config.json`.

    Criterio de aceptación: **cero rotas**.
  </action>
  <verify>
    - Bloque `fetch HEAD` retorna todas 200.
    - `pnpm format` limpio tras cualquier cambio JSON.
  </verify>
  <done>
    11 URLs del módulo Tickets responden 200.
  </done>
</task>

<task type="auto">
  <name>COMMIT Ola 3: tickets detail + buy flow + events dispatch QR</name>
  <files>n/a (git)</files>
  <action>
    ```bash
    git add src/components/listings/listing-detail.tsx \
            src/components/tickets/ticket-detail-with-buy.tsx \
            src/app/\(kiosk\)/home/\[module\]/\[slug\]/page.tsx \
            clients/default/config.json
    git commit -m "$(cat <<'EOF'
    feat(tickets): detail + BUY TICKET flow + Events dispatcha QR si hay .ticket

    Ola 3/4 Fase 3.11.

    - SecondaryCta acepta onClick además de href (mutuamente exclusivos)
    - TicketDetailWithBuy wrappea ListingDetail con CTA disparando kiosk:ticket-purchase-open
    - Rama 'tickets' en [slug]/page.tsx monta TicketsModule + TicketDetailWithBuy + QrPurchaseHost
    - Events con event.ticket presente también usa QrPurchaseHost (reemplaza ticketsUrl legacy)
    - URLs de imágenes verificadas 200
    EOF
    )"
    ```
  </action>
  <verify>
    - `git log -1 --oneline` muestra el commit.
  </verify>
  <done>
    Commit de Ola 3 hecho.
  </done>
</task>

---

## Ola 4 — QA + cierre

<task type="auto">
  <name>T19: Verificación visual Playwright MCP — 5 screenshots críticos</name>
  <files>.planning/verifications/3-11-*.png</files>
  <action>
    Con Playwright MCP abierto (`pnpm kiosk:dev` corriendo), capturar:

    1. `/home/tickets` listing → `.planning/verifications/3-11-tickets-listing.png`. Verificar: WeekPicker visible, ≥1 ticket-card visible en la fecha con tickets, badge de precio pill blanco top-right sobre cover.

    2. `/home/tickets/<slug-con-ticket>` detail → `.planning/verifications/3-11-tickets-detail.png`. Verificar: ListingDetail render + CTA azul "BUY TICKET" visible en la secondaryCta position.

    3. Trigger `kiosk:ticket-purchase-open` → screenshot QR modal → `.planning/verifications/3-11-tickets-qr-modal.png`. Verificar: header azul con title uppercase + priceDisplay visible encima del QR + phone input + SEND.

    4. Completar phone (10 dígitos via keypad) + SEND → screenshot sent confirmation → `.planning/verifications/3-11-tickets-sent.png`. Verificar: círculo lime + "Link sent!" + "Check your phone to complete the purchase."

    5. `/home/events/<mismo-slug>` → trigger BUY TICKET → screenshot → `.planning/verifications/3-11-events-ticket-reuse.png`. Verificar: mismo QR modal funciona desde Events.

    Para estabilizar screenshots del QR modal, temporalmente aumentar `autoCloseMs` en `src/components/shared/sent-confirmation.tsx` a 30000 durante la captura; revertir a 3000 al terminar.
  </action>
  <verify>
    - 5 archivos PNG existen en `.planning/verifications/`.
    - `git status` muestra los 5 archivos como untracked.
  </verify>
  <done>
    5 screenshots capturados y verificados visualmente OK.
  </done>
</task>

<task type="auto">
  <name>T20: Auditor white-label + escribir 3-11-SUMMARY.md + commit cierre</name>
  <files>.planning/3-11-SUMMARY.md, .planning/STATE.md</files>
  <action>
    **1. Ejecutar `auditor-white-label` subagent** sobre:
    - `src/components/tickets/`
    - `src/components/shared/qr-*.tsx` + `sent-confirmation.tsx`
    - `src/lib/tickets.ts`
    - Ramas `tickets` en `[module]/page.tsx` y `[slug]/page.tsx`
    - Modificación de `SecondaryCta` en `listing-detail.tsx`

    Excepciones aceptadas a documentar (no fixes):
    - `#004f8b` header del QR modal — heredado del chrome aprobado en Passes (Fase 3.10).
    - `#0a1e3a` / `#ffffff` del QR — alto contraste escaneable, fijos por diseño.
    - `#1796d6` del SEND button — del send-modal-chrome compartido.
    - Grises `#9a9a9a`, `#bfbfbf`, `#d0d0d0`, `#fff` (inputs) — heredados del send-modal-chrome.

    Arreglar cualquier hallazgo real no listado en las excepciones (siguiendo patrón de Passes Fase 3.10).

    **2. Escribir `.planning/3-11-SUMMARY.md`** siguiendo el formato de `.planning/3-10-SUMMARY.md` con secciones: Hecho (por ola), Verificado, Pendiente / siguiente, Decisiones, Fase. Fecha 2026-04-22 o la actual al ejecutar.

    **3. Actualizar `.planning/STATE.md`** añadiendo entrada de sesión nueva al final del archivo (siguiendo el formato de la plantilla). Actualizar "Estado actual" con "Fase activa: 3.11 Tickets cerrada" y "Siguiente acción" con la próxima fase (Fase 4 — primer cliente real, o siguiente módulo).

    **4. Commit** cierre:
    ```bash
    git add .planning/3-11-SUMMARY.md .planning/STATE.md .planning/verifications/3-11-*.png
    git commit -m "$(cat <<'EOF'
    docs(tickets): SUMMARY + verificaciones + STATE cierre Fase 3.11

    Ola 4/4 Fase 3.11.

    - .planning/3-11-SUMMARY.md cierre formal
    - 5 screenshots .planning/verifications/3-11-*.png
    - STATE.md entrada sesión + siguiente acción
    - auditor white-label ejecutado, excepciones documentadas
    EOF
    )"
    ```
  </action>
  <verify>
    - `.planning/3-11-SUMMARY.md` existe y tiene secciones completas.
    - `.planning/STATE.md` termina con entrada nueva de esta sesión.
    - `git log -1 --oneline` muestra el commit de cierre.
    - `git status` limpio.
    - `pnpm check` pasa limpio en el repo final.
  </verify>
  <done>
    Fase 3.11 cerrada con SUMMARY + STATE + auditor + commit final.
  </done>
</task>

---

## Orden de dependencias

```
Ola 1:  T1 → T2 → T3 → T4 → T5 → T6 → T7 → COMMIT
Ola 2:  T8 → T9 → T10 → T11 → T12 → T13 → COMMIT
Ola 3:  T14 → T15 → T16 → T17 → T18 → COMMIT
Ola 4:  T19 → T20 (incluye commit final)
```

**Checkpoints entre olas:** después de cada commit, validar `pnpm check` + Playwright smoke test de la feature de esa ola antes de avanzar.

**Rollback strategy:** cada commit es atómico. Un `git reset --hard HEAD~1` revierte una ola entera sin dejar estados inconsistentes.
