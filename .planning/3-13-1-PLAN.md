# Fase 3.13 — Plan atómico Trails module

> Ejecutado en sesión 2026-04-23 (post-aprobación de plan mode).
> 4 olas consolidadas en un único pase (ver `3-13-SUMMARY.md`).

<phase id="3.13" name="Trails module">

<task type="auto" wave="1">
  <name>Tipos + data layer + seed config</name>
  <files>
    src/lib/config.ts,
    src/lib/trails.ts,
    src/lib/favorites.ts,
    clients/default/config.json,
    clients/_template/config.json,
    clients/demo-cliente-a/config.json
  </files>
  <action>
    Añadir `Trail`, `TrailConsiderations`, `TrailDifficulty`, `TrailType`,
    `HomeTrailsModule` a `src/lib/config.ts` + union.
    Crear `src/lib/trails.ts` con TrailFilterState + EMPTY_TRAILS_FILTER +
    applyTrailsFilter + searchTrails + trailToListing.
    Exportar `useTrailFavorites` desde favorites.ts.
    Poblar 15 trails seed (Arizona classics) con GeoJSON embed en
    clients/default/config.json. Mirror de 21 textos `trails_*` a los 3
    clientes (default EN, _template EN, demo-cliente-a ES).
  </action>
  <verify>
    pnpm typecheck limpio. JSON parseable en los 3 configs.
  </verify>
  <done>
    Tipos accesibles sin errores. 15 trails leíbles vía getConfig().
  </done>
</task>

<task type="auto" wave="2">
  <name>Listing + toolbar + filter 3 secciones + ruta</name>
  <files>
    src/components/trails/trails-module.tsx,
    src/components/trails/trails-filter-overlay.tsx,
    src/app/(kiosk)/home/[module]/page.tsx,
    src/app/(kiosk)/home/[module]/[slug]/page.tsx
  </files>
  <action>
    TrailsModule compose: HomeHeader + ListingsToolbar (reusado) +
    ListingsGrid (reusado via trailToListing adapter) + TrailsFilterOverlay
    (3 secciones) + SortOverlay reusado + SearchOverlay reusado.
    Branch `case 'trails'` en [module]/page.tsx con AdsSlot.
    Guard temporal en [slug]/page.tsx mientras Ola 3 se implementa.
  </action>
  <verify>
    Playwright MCP `/home/trails` renderea 15 cards sorted by popularity.
    pnpm check limpio.
  </verify>
  <done>
    Grid + filter + search + sort funcionan. Favoritos persisten en
    kiosk_trail_favorites.
  </done>
</task>

<task type="auto" wave="3">
  <name>Detail con tabs de mapa + Considerations</name>
  <files>
    src/components/trails/trail-detail.tsx,
    src/components/trails/trail-map-tabs.tsx,
    src/components/trails/considerations-panel.tsx,
    src/components/listings/listing-detail.tsx,
    src/app/(kiosk)/home/[module]/[slug]/page.tsx
  </files>
  <action>
    Extender ListingDetail con props `mapSlot?: ReactNode` y `cardHeight?:
    number`, y aceptar `favoritesKind='trail'`.
    TrailMapTabs: tabs [Default | Trail], un solo MapboxMap, source + layer
    GeoJSON LineString con visibility toggle + fit bounds al activar trail.
    ConsiderationsPanel: grid 2-col de 6 rows opcionales con iconos SVG.
    TrailDetail wrapper que inyecta mapSlot + extraDetails + cardHeight=1780.
    Branch `kind === 'trails'` en [slug]/page.tsx con TrailsModule + TrailDetail + AdsSlot.
  </action>
  <verify>
    Playwright MCP detail muestra tabs + mapa + 6 considerations. Click en
    Trail Map dibuja polyline y hace fit al bbox. Default Map vuelve al pin.
  </verify>
  <done>
    Detail pixel-similar al patrón establecido. Tabs funcionan. Card
    accomoda los 6 rows.
  </done>
</task>

<task type="auto" wave="4">
  <name>QA cierre + docs</name>
  <files>
    .planning/3-13-SUMMARY.md,
    .planning/3-13-COVERAGE.md,
    .planning/3-13-1-PLAN.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    docs/superpowers/specs/2026-04-23-trails-module-design.md
  </files>
  <action>
    Spec formal, SUMMARY, COVERAGE. Actualizar STATE y ROADMAP con Fase
    3.13 cerrada. Documentar TODOs (map integration, GET DIRECTIONS modal).
  </action>
  <verify>
    STATE + ROADMAP reflejan Fase 3.13 cerrada.
  </verify>
  <done>
    Commit único con los 4 tasks.
  </done>
</task>

</phase>
