# Fase 3.14 — Plan atómico Guestbook module

> Ejecutado en sesión 2026-04-23. 5 olas consolidadas en un pase.

<phase id="3.14" name="Guestbook module">

<task type="auto" wave="1">
  <name>Tipos + helpers + seed</name>
  <files>
    src/lib/config.ts, src/lib/guestbook-geo.ts, src/lib/guestbook-bbox.ts,
    src/lib/guestbook-store.ts,
    clients/default/config.json, clients/_template/config.json,
    clients/demo-cliente-a/config.json,
    clients/default/assets/guestbook/pins/*.svg
  </files>
  <action>
    Tipos Trail/TrailConsiderations/HomeGuestbookModule + union.
    geocodeZip Mapbox API con fallback. filterPinsByProximity bbox aprox.
    useGuestbookUserPins sessionStorage. 15 seedPins + 5 pinCatalog +
    20 países + 22 textos. 5 SVGs de pins.
  </action>
  <verify>typecheck + json parse limpios.</verify>
  <done>Data disponible vía getConfig.</done>
</task>

<task type="auto" wave="2">
  <name>Start + Form screens + module state machine</name>
  <files>
    src/components/guestbook/guestbook-module.tsx,
    src/components/guestbook/guestbook-start-screen.tsx,
    src/components/guestbook/guestbook-form-screen.tsx,
    src/components/guestbook/guestbook-form-fields.tsx,
    src/components/guestbook/guestbook-country-dropdown.tsx,
    src/app/(kiosk)/home/[module]/page.tsx,
    src/app/(kiosk)/home/[module]/[slug]/page.tsx
  </files>
  <action>
    GuestbookModule con phase state. Start + Form screens con QWERTY +
    NumericKeypad + country dropdown. Validación. Rama case 'guestbook'.
  </action>
  <verify>Playwright MCP start screen + form screen visibles.</verify>
  <done>Click START avanza a form; form valida campos.</done>
</task>

<task type="auto" wave="3">
  <name>GuestbookGlobeCanvas + transición</name>
  <files>src/components/guestbook/guestbook-globe-canvas.tsx</files>
  <action>
    MapboxMap con projection globe + satellite style. ForwardRef con
    imperative flyToZip (resuelve en moveend). Switch a streets-v12
    al terminar.
  </action>
  <verify>Globo visible en start/form con projection globe; flyTo anima.</verify>
  <done>Globe renderea atmosphere + star field.</done>
</task>

<task type="auto" wave="4">
  <name>Map screen + drag&drop + comment modal</name>
  <files>
    src/components/guestbook/guestbook-map-screen.tsx,
    src/components/guestbook/guestbook-pin-rail.tsx,
    src/components/guestbook/guestbook-pin-comment-modal.tsx
  </files>
  <action>
    MapScreen renderea seedPins como mapboxgl.Marker DOM.
    PinRail con pointer events + clone visual fijo. onDrop con
    map.unproject. Comment modal con QWERTY + CONFIRM. FINISH botón
    olive cuando hay pin colocado.
  </action>
  <verify>Map + rail + modal renderean. Drag funciona.</verify>
  <done>Flujo completo end-to-end testeable manualmente.</done>
</task>

<task type="auto" wave="5">
  <name>QA cierre + docs</name>
  <files>
    .planning/3-14-SUMMARY.md, .planning/3-14-COVERAGE.md,
    .planning/3-14-1-PLAN.md, .planning/STATE.md, .planning/ROADMAP.md,
    docs/superpowers/specs/2026-04-23-guestbook-module-design.md
  </files>
  <action>Spec + docs + STATE + ROADMAP. Screenshots de start + form.</action>
  <verify>STATE actualizado, Fase 3.14 cerrada en ROADMAP.</verify>
  <done>Commit único con los 5 waves.</done>
</task>

</phase>
