/**
 * Studio UI primitives — barrel export.
 *
 * Importar como:
 *   import { Field, TextInput, Select, ColorPicker } from '../ui';
 *
 * Hallazgo #15 / Tema B.2 del audit panorámico Studio (2026-05-05).
 */

export { Checkbox, ColorPicker, Field, NumberInput, Select, Textarea, TextInput } from './Field';
