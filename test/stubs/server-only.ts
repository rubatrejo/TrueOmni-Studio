/**
 * Stub de `server-only` para Vitest. El paquete real lanza si se importa fuera
 * de un React Server Component; en tests de lógica pura (bootstrap, schemas) lo
 * neutralizamos con este módulo vacío (alias en vitest.config.ts).
 */
export {};
